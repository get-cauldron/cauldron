/** Crystallize command — finalizes a seed spec from a completed interview. */
export async function crystallizeCommand(): Promise<void> {
  console.log('Not implemented: crystallize');
  process.exit(0);
}
