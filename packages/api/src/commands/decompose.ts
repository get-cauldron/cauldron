/** Decompose command — runs two-pass LLM decomposition on a crystallized seed. */
export async function decomposeCommand(): Promise<void> {
  console.log('Not implemented: decompose');
  process.exit(0);
}
