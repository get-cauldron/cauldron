---
phase: 06.2-testing-and-tuning-the-dogfood-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/interview/scorer.ts
  - packages/engine/src/interview/perspectives.ts
  - packages/engine/src/interview/__tests__/scorer.test.ts
  - packages/engine/src/interview/__tests__/perspectives.test.ts
autonomous: true
requirements: [D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "Scorer returns overall >= 0.5 for a transcript where the user provided concrete, specific answers to 3+ turns"
    - "Perspective selector advances past researcher/simplifier/breadth-keeper when overall score crosses 0.4"
    - "When one dimension is low while others are high, the weakest-dimension specialist perspective activates"
    - "All existing 64 interview tests continue to pass after changes"
  artifacts:
    - path: "packages/engine/src/interview/scorer.ts"
      provides: "Calibrated SCORER_SYSTEM_PROMPT with anchored scoring examples and recency-weighted prompt builder"
      contains: "SCORER_SYSTEM_PROMPT"
    - path: "packages/engine/src/interview/perspectives.ts"
      provides: "Dimension-aware perspective activation with convergence pressure"
      exports: ["selectActivePerspectives"]
    - path: "packages/engine/src/interview/__tests__/scorer.test.ts"
      provides: "Regression tests for scorer calibration"
      contains: "calibration"
    - path: "packages/engine/src/interview/__tests__/perspectives.test.ts"
      provides: "Regression tests for dimension-aware perspective routing"
      contains: "dimension"
  key_links:
    - from: "packages/engine/src/interview/scorer.ts"
      to: "LLM gateway"
      via: "gateway.generateObject with calibrated system prompt"
      pattern: "SCORER_SYSTEM_PROMPT"
    - from: "packages/engine/src/interview/perspectives.ts"
      to: "packages/engine/src/interview/scorer.ts"
      via: "Uses AmbiguityScores.overall + per-dimension scores to select perspectives"
      pattern: "selectActivePerspectives"
---

<objective>
Fix the three confirmed interview convergence bugs: (1) scorer underscoring concrete answers, (2) perspective activation stuck on researcher, (3) no convergence pressure toward weak dimensions. Per D-03, fixes go directly into Phase 3 engine source. Per D-04, every fix gets a regression test. Per D-05, start empirical (calibrate scorer prompt, observe results). Per D-06, addresses all three known dry-run issues.

Purpose: Unblock interview convergence so the scorer rewards concrete answers, perspectives rotate through all 5 types, and weak dimensions get targeted attention.
Output: Patched scorer.ts and perspectives.ts with regression tests proving correct behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-testing-and-tuning-the-dogfood-process/06.2-CONTEXT.md
@.planning/phases/06.2-testing-and-tuning-the-dogfood-process/06.2-RESEARCH.md

@packages/engine/src/interview/scorer.ts
@packages/engine/src/interview/perspectives.ts
@packages/engine/src/interview/types.ts
@packages/engine/src/interview/__tests__/scorer.test.ts
@packages/engine/src/interview/__tests__/perspectives.test.ts

<interfaces>
<!-- Key types the executor needs -->

From packages/engine/src/interview/types.ts:
```typescript
export interface AmbiguityScores {
  goalClarity: number;       // [0, 1]
  constraintClarity: number; // [0, 1]
  successCriteriaClarity: number; // [0, 1]
  contextClarity?: number;   // [0, 1] — only present in brownfield mode
  overall: number;           // weighted score
  reasoning: string;         // brief LLM rationale
}

export type PerspectiveName = 'researcher' | 'simplifier' | 'architect' | 'breadth-keeper' | 'seed-closer';
```

From packages/engine/src/interview/scorer.ts:
```typescript
export function computeWeightedScore(scores, mode): number;
export function buildScorerPrompt(transcript: InterviewTurn[], mode: InterviewMode): string;
export async function scoreTranscript(gateway, transcript, mode, projectId, previousScores, config): Promise<AmbiguityScores>;
export function validateScoreRules(current, previous): ScoreValidationResult;
export const SCORER_SYSTEM_PROMPT: string;
```

From packages/engine/src/interview/perspectives.ts:
```typescript
export function selectActivePerspectives(previousScores: AmbiguityScores | null, turnCount: number): PerspectiveName[];
export async function runActivePerspectives(gateway, transcript, previousScores, projectId, turnCount, config): Promise<PerspectiveCandidate[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibrate scorer system prompt with anchored examples and add recency weighting to prompt builder</name>
  <files>packages/engine/src/interview/scorer.ts, packages/engine/src/interview/__tests__/scorer.test.ts</files>
  <read_first>
    - packages/engine/src/interview/scorer.ts
    - packages/engine/src/interview/__tests__/scorer.test.ts
    - packages/engine/src/interview/types.ts
  </read_first>
  <behavior>
    - Test: buildScorerPrompt with 5+ turns includes "MOST RECENT ANSWERS (weight heavily):" marker before last 2 turns
    - Test: buildScorerPrompt with 1 turn still formats correctly (no crash on short transcripts)
    - Test: SCORER_SYSTEM_PROMPT contains "0.7-0.9" anchoring text (calibration guidance present)
    - Test: SCORER_SYSTEM_PROMPT contains "specific, testable" (concrete-answer reward signal present)
    - Test: existing computeWeightedScore tests still pass unchanged
    - Test: existing validateScoreRules tests still pass unchanged
    - Test: existing scoreTranscript mock tests still pass (gateway call signature unchanged)
  </behavior>
  <action>
    **Per D-03 (fix at source) and D-06 (known issues a and b):**

    1. **Rewrite `SCORER_SYSTEM_PROMPT` in `scorer.ts`** — replace the current underdirected prompt with a calibrated version containing explicit scoring anchors. The new prompt must include:
       - Anchoring examples: "Score 0.8-1.0: The user has given specific, testable, unambiguous answers (e.g., 'TypeScript, CLI tool, Unix-only, dry-run mode required'). Score 0.4-0.7: Partial clarity — the user has expressed intent but details are vague or missing (e.g., 'some kind of file tool'). Score 0.0-0.3: Little or no information provided for this dimension."
       - Per-dimension definitions: goal clarity = "What is being built and for whom?", constraint clarity = "What technical/business limits apply?", success criteria clarity = "How will we know it works? Are acceptance criteria testable?"
       - Instruction: "Score based on the MOST RECENT state of knowledge. If the user gave a specific answer in turn 8 that resolves a question from turn 2, score the dimension based on turn 8's answer, not turn 2's ambiguity."
       - Keep the existing brownfield contextClarity dimension definition.

    2. **Update `buildScorerPrompt()` in `scorer.ts`** — add recency weighting to the transcript formatting:
       - When `transcript.length >= 3`, split turns into two sections:
         - "EARLIER CONTEXT:" for turns `0` through `transcript.length - 3`
         - "MOST RECENT ANSWERS (weight heavily):" for the last 2 turns
       - When `transcript.length < 3`, format all turns under a single section (no splitting).
       - The empty-transcript special case (`transcript.length === 0`) remains unchanged.

    3. **Do NOT change**: `computeWeightedScore`, `validateScoreRules`, `scoreTranscript` function signatures, `greenfieldScoresSchema`, `brownfieldScoresSchema`, or the `temperature: 0` setting.

    4. **Add regression tests in `scorer.test.ts`**:
       - Test: `SCORER_SYSTEM_PROMPT` contains the string `"0.8-1.0"` (anchoring present)
       - Test: `SCORER_SYSTEM_PROMPT` contains the string `"specific, testable"` (concrete-answer signal)
       - Test: `buildScorerPrompt` with 5 turns returns a string containing `"MOST RECENT ANSWERS"`
       - Test: `buildScorerPrompt` with 2 turns does NOT contain `"MOST RECENT ANSWERS"` (too few turns for splitting)
       - Test: `buildScorerPrompt` with 0 turns returns the existing "No interview turns yet" message (unchanged)

    Per D-04, these tests serve as regression guards so future prompt edits don't silently regress calibration.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm exec vitest run packages/engine/src/interview/__tests__/scorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scorer.ts SCORER_SYSTEM_PROMPT contains the substring "0.8-1.0"
    - scorer.ts SCORER_SYSTEM_PROMPT contains the substring "specific, testable"
    - scorer.ts SCORER_SYSTEM_PROMPT contains the substring "MOST RECENT state of knowledge"
    - scorer.ts buildScorerPrompt function contains the string "MOST RECENT ANSWERS"
    - scorer.test.ts contains test descriptions matching "calibration" or "recency" or "MOST RECENT"
    - All tests in scorer.test.ts pass (exit code 0)
    - computeWeightedScore function body is UNCHANGED
    - validateScoreRules function body is UNCHANGED
    - scoreTranscript function signature is UNCHANGED (same parameters, same return type)
  </acceptance_criteria>
  <done>SCORER_SYSTEM_PROMPT has explicit calibration anchors (0.8-1.0 for specific answers, 0.4-0.7 for partial, 0.0-0.3 for missing). buildScorerPrompt marks last 2 turns as "MOST RECENT ANSWERS (weight heavily)" when transcript has 3+ turns. All existing scorer tests pass plus new regression tests for calibration and recency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dimension-aware convergence pressure to perspective activation and regression tests</name>
  <files>packages/engine/src/interview/perspectives.ts, packages/engine/src/interview/__tests__/perspectives.test.ts</files>
  <read_first>
    - packages/engine/src/interview/perspectives.ts
    - packages/engine/src/interview/__tests__/perspectives.test.ts
    - packages/engine/src/interview/types.ts
  </read_first>
  <behavior>
    - Test: mid-turn (overall=0.5) with low constraintClarity (0.2) and high goalClarity (0.8) includes 'breadth-keeper' in result
    - Test: mid-turn (overall=0.5) with low successCriteriaClarity (0.15) includes 'seed-closer' in result
    - Test: late-turn (overall=0.75) with successCriteriaClarity < 0.5 includes 'seed-closer' in result (already in late set, just verify)
    - Test: early-turn (overall=0.2) still returns ['researcher', 'simplifier', 'breadth-keeper'] (unchanged)
    - Test: first-turn (null scores) still returns ['researcher', 'simplifier', 'breadth-keeper'] (unchanged)
    - Test: all existing selectActivePerspectives tests still pass
  </behavior>
  <action>
    **Per D-03 (fix at source), D-04 (regression test per fix), and D-06 (known issues a, b, c):**

    1. **Enhance `selectActivePerspectives()` in `perspectives.ts`** — add dimension-aware routing in the mid-turn block (0.4 <= overall < 0.7):
       - Current mid-turn logic: `['architect', 'breadth-keeper']` + researcher or simplifier based on goalClarity being lowest.
       - New mid-turn logic: Keep `'architect'` as base. For the 2nd and 3rd slots, pick the two perspectives most relevant to the weakest dimensions:
         - If `successCriteriaClarity` is the lowest dimension (and < 0.5): include `'seed-closer'`
         - If `constraintClarity` is the lowest dimension (and < 0.5): include `'breadth-keeper'`
         - If `goalClarity` is the lowest dimension (and < 0.5): include `'researcher'`
         - Fill remaining slot(s) from `['breadth-keeper', 'simplifier']` (in that order), avoiding duplicates
         - If no dimension is below 0.5 (all mid-range), keep the existing behavior: `['architect', 'breadth-keeper', 'simplifier']`

    2. **Enhance late-turn logic (overall >= 0.7):**
       - Current: always returns `['seed-closer', 'architect']`
       - New: if any dimension is below 0.5, add that dimension's specialist as a 3rd perspective:
         - `successCriteriaClarity < 0.5` → add `'seed-closer'` (already present, so add `'simplifier'` to help clarify)
         - `constraintClarity < 0.5` → add `'breadth-keeper'`
         - `goalClarity < 0.5` → add `'researcher'`
         - If no dimension below 0.5, keep the 2-perspective set `['seed-closer', 'architect']`

    3. **Do NOT change**: `PERSPECTIVE_PROMPTS`, `perspectiveCandidateSchema`, `buildPerspectivePrompt`, `runActivePerspectives` function signatures, or the early-turn block (overall < 0.4 or null/turnCount=0).

    4. **Add regression tests in `perspectives.test.ts`**:
       - Test "mid-turn: low constraintClarity targets breadth-keeper": scores `{overall: 0.5, goalClarity: 0.8, constraintClarity: 0.2, successCriteriaClarity: 0.7}`, turnCount=5 → result includes `'breadth-keeper'`
       - Test "mid-turn: low successCriteriaClarity targets seed-closer": scores `{overall: 0.5, goalClarity: 0.7, constraintClarity: 0.8, successCriteriaClarity: 0.15}`, turnCount=5 → result includes `'seed-closer'`
       - Test "mid-turn: all dimensions above 0.5 defaults to architect+breadth-keeper+simplifier": scores `{overall: 0.55, goalClarity: 0.6, constraintClarity: 0.55, successCriteriaClarity: 0.5}`, turnCount=5 → result is `['architect', 'breadth-keeper', 'simplifier']`
       - Test "late-turn: low constraintClarity adds breadth-keeper": scores `{overall: 0.75, goalClarity: 0.9, constraintClarity: 0.3, successCriteriaClarity: 0.8}`, turnCount=10 → result includes `'breadth-keeper'`
       - Test "early-turn unchanged": scores `{overall: 0.2, ...}`, turnCount=3 → `['researcher', 'simplifier', 'breadth-keeper']`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm exec vitest run packages/engine/src/interview/__tests__/perspectives.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - perspectives.ts selectActivePerspectives function contains the string "seed-closer" in the mid-turn block (0.4 <= overall < 0.7 section)
    - perspectives.ts selectActivePerspectives function contains the comparison "successCriteriaClarity" checking against a threshold in the mid-turn block
    - perspectives.ts late-turn block references per-dimension scores (not just overall)
    - perspectives.test.ts contains test names matching "low constraintClarity" and "low successCriteriaClarity"
    - perspectives.test.ts contains at least 5 new test cases (dimension-aware routing)
    - All tests in perspectives.test.ts pass (exit code 0)
    - PERSPECTIVE_PROMPTS object is UNCHANGED (same 5 entries)
    - runActivePerspectives function signature is UNCHANGED
    - Full interview test suite passes: `pnpm exec vitest run packages/engine/src/interview` exits 0 with 64+ tests passing
  </acceptance_criteria>
  <done>selectActivePerspectives routes to dimension-specific specialist perspectives in mid and late turns. Mid-turn with low successCriteriaClarity includes seed-closer. Mid-turn with low constraintClarity includes breadth-keeper. Late-turn with any dimension below 0.5 adds a third specialist. All existing perspective tests pass plus 5+ new dimension-aware tests.</done>
</task>

</tasks>

<verification>
1. Full interview test suite: `pnpm exec vitest run packages/engine/src/interview` — must exit 0 with 64+ tests (original 64 + new regression tests)
2. Full workspace test suite: `pnpm exec vitest run --workspace vitest.workspace.ts` — must exit 0 (no regressions in other packages)
3. TypeScript check: `pnpm exec tsc --noEmit -p packages/engine/tsconfig.json` — must exit 0
</verification>

<success_criteria>
- SCORER_SYSTEM_PROMPT contains calibration anchors with explicit score ranges and examples
- buildScorerPrompt marks recent turns for recency weighting
- selectActivePerspectives uses per-dimension scores to route to specialist perspectives in mid/late turns
- All 64+ existing tests pass, plus new regression tests for each fix
- No changes to function signatures, schema types, or gateway call patterns
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-testing-and-tuning-the-dogfood-process/06.2-01-SUMMARY.md`
</output>
