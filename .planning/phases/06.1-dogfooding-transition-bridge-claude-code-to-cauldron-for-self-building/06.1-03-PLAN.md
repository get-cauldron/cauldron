---
phase: 06.1-dogfooding-transition
plan: 03
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - packages/api/src/commands/interview.ts
  - packages/api/src/commands/crystallize.ts
  - packages/api/src/context-bridge.ts
  - packages/api/src/__tests__/interview-command.test.ts
  - packages/api/src/__tests__/context-bridge.test.ts
autonomous: true
requirements:
  - D-06
  - D-07
  - D-10
  - D-16

must_haves:
  truths:
    - "Interview command reads .planning/ files and injects them as prior context before the FSM starts asking questions"
    - "Interview command drives the FSM turn-by-turn until crystallization phase"
    - "Interview outputs a seed draft JSON file for user review"
    - "Crystallize command reads the seed draft file and calls approveAndCrystallize"
    - "GSD requirement IDs (EVOL-01 etc.) are preserved in seed acceptance criteria"
  artifacts:
    - path: "packages/api/src/commands/interview.ts"
      provides: "Interactive interview command driving InterviewFSM"
      contains: "startOrResume"
    - path: "packages/api/src/commands/crystallize.ts"
      provides: "Seed crystallization from review file"
      contains: "approveAndCrystallize"
    - path: "packages/api/src/context-bridge.ts"
      provides: "GSD artifact reader for .planning/ directory"
      exports: ["readPlanningArtifacts"]
  key_links:
    - from: "packages/api/src/commands/interview.ts"
      to: "packages/engine/src/interview/fsm.ts"
      via: "InterviewFSM.startOrResume + submitAnswer"
      pattern: "InterviewFSM"
    - from: "packages/api/src/context-bridge.ts"
      to: ".planning/"
      via: "fs.readFileSync of PROJECT.md, REQUIREMENTS.md, CONTEXT.md"
      pattern: "readFileSync.*\\.planning"
    - from: "packages/api/src/commands/crystallize.ts"
      to: "packages/api/src/review/seed-writer.ts"
      via: "readSeedDraft"
      pattern: "readSeedDraft"
---

<objective>
Implement the interview command with GSD context bridging and the crystallize command for file-based seed review.

Purpose: The interview is the pipeline entry point. GSD bridging (D-07) lets the interview skip already-decided questions by reading .planning/ artifacts. The crystallize command completes the edit-then-approve flow (D-16).
Output: Working interview + crystallize commands that drive the Socratic interview FSM with prior planning context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-CONTEXT.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-RESEARCH.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-01-SUMMARY.md

<interfaces>
<!-- From packages/engine/src/interview/fsm.ts -->
```typescript
export class InterviewFSM {
  constructor(db: DbClient, gateway: LLMGateway, config: GatewayConfig, logger: Logger);
  async startOrResume(projectId: string, opts?: { mode?: InterviewMode; projectPath?: string }): Promise<Interview>;
  async submitAnswer(interviewId: string, projectId: string, input: { userAnswer: string }): Promise<InterviewTurn>;
  async generateSummary(interviewId: string, projectId: string): Promise<SeedSummary>;
  async approveAndCrystallize(interviewId: string, projectId: string): Promise<Seed>;
}
```

<!-- From packages/api/src/bootstrap.ts (Plan 01) -->
```typescript
export interface BootstrapDeps { db, gateway, inngest, logger, config }
export async function bootstrap(projectRoot: string): Promise<BootstrapDeps>;
```

<!-- From packages/api/src/review/seed-writer.ts (Plan 02) -->
```typescript
export async function writeSeedDraft(projectRoot: string, projectId: string, summary: SeedSummary): Promise<string>;
export async function readSeedDraft(projectRoot: string, projectId: string): Promise<SeedSummary>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GSD context bridge and interview command</name>
  <files>
    packages/api/src/context-bridge.ts,
    packages/api/src/commands/interview.ts,
    packages/api/src/__tests__/context-bridge.test.ts,
    packages/api/src/__tests__/interview-command.test.ts
  </files>
  <read_first>
    packages/engine/src/interview/fsm.ts,
    packages/engine/src/interview/types.ts,
    packages/engine/src/interview/perspectives.ts,
    packages/shared/src/db/schema/interview.ts,
    packages/api/src/bootstrap.ts,
    packages/api/src/cli.ts,
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: readPlanningArtifacts reads PROJECT.md, REQUIREMENTS.md, and phase-specific CONTEXT.md from .planning/ and returns concatenated string
    - Test 2: readPlanningArtifacts returns empty string when .planning/ directory does not exist
    - Test 3: readPlanningArtifacts includes requirement IDs (EVOL-01, etc.) in output per D-10
    - Test 4: interviewCommand creates InterviewFSM and calls startOrResume with brownfield mode when .planning/ exists
    - Test 5: interviewCommand injects prior context as a system transcript entry before first submitAnswer
  </behavior>
  <action>
    1. Create `packages/api/src/context-bridge.ts` (per D-07, D-10):
       - Export `async function readPlanningArtifacts(projectRoot: string, phaseId?: string): Promise<string>`
       - Read the following files if they exist (use `fs.readFileSync` with try/catch for missing files):
         - `{projectRoot}/.planning/PROJECT.md` — project vision and constraints
         - `{projectRoot}/.planning/REQUIREMENTS.md` — acceptance criteria with IDs (D-10)
         - `{projectRoot}/.planning/phases/{phaseId}*/*-CONTEXT.md` — phase-specific decisions (if phaseId provided, glob for the matching phase dir)
       - Concatenate with section headers: `## Project Context\n{project}\n\n## Requirements\n{requirements}\n\n## Phase Decisions\n{context}`
       - Return the concatenated string. Return empty string if no files found.
       - Export `function extractRequirementIds(requirementsContent: string): string[]` — regex to find all IDs matching pattern `/[A-Z]+-\d+/g` (e.g., EVOL-01, WEB-03). Used to verify D-10 traceability.

    2. Replace `packages/api/src/commands/interview.ts` stub (per D-06, D-07):
       - Export `async function interviewCommand(deps: BootstrapDeps, args: string[]): Promise<void>`
       - Parse args: `--project-id` (required), `--project-root` (defaults to cwd), `--phase` (optional, for context bridge phase filtering)
       - Call `readPlanningArtifacts(projectRoot, phase)` to get prior context
       - Create FSM: `const fsm = new InterviewFSM(deps.db, deps.gateway, deps.config, deps.logger)`
       - Start interview: `const interview = await fsm.startOrResume(projectId, { mode: 'brownfield', projectPath: projectRoot })`
       - If prior context is non-empty, inject it into the interview transcript as a pre-populated system turn. Approach: call `deps.db.update(interviews).set({ transcript: [{ role: 'system', type: 'prior_context', content: priorContext }] }).where(eq(interviews.id, interview.id))` — this seeds the transcript JSONB with context the FSM's perspective selection and scoring will have access to but won't score (the scorer checks `role === 'user'` for scoring). Read the actual scoring logic in fsm.ts to confirm this assumption.
       - **IMPORTANT**: Read the actual `submitAnswer` implementation to verify that the scorer only scores `role: 'user'` turns. If it scores all turns, use a different injection approach — add the prior context to the first user answer as a preamble, or extend `startOrResume` to accept a `priorContext` parameter (Pitfall 6 from research).
       - Enter turn loop: read user input from stdin (use `process.stdin` readline), call `fsm.submitAnswer(interview.id, projectId, { userAnswer: input })`, print the next question and current ambiguity score
       - When turn returns phase `reviewing`: call `fsm.generateSummary(interview.id, projectId)`, write seed draft via `writeSeedDraft(projectRoot, projectId, summary)`
       - Print: "Seed draft written to {path}. Review and edit, then run: pnpm exec tsx packages/api/src/cli.ts crystallize --project-id {projectId}"
       - Exit 0

    3. Create tests:
       - `context-bridge.test.ts`: create temp dir with mock .planning/ files, verify readPlanningArtifacts reads and concatenates them, verify extractRequirementIds finds IDs
       - `interview-command.test.ts`: mock InterviewFSM, mock bootstrap deps, verify the command calls startOrResume, injects prior context, and writes seed draft on review phase

    All files use `.js` extensions on relative imports.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @cauldron/api test && pnpm --filter @cauldron/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/context-bridge.ts contains `export async function readPlanningArtifacts`
    - packages/api/src/context-bridge.ts contains `PROJECT.md`
    - packages/api/src/context-bridge.ts contains `REQUIREMENTS.md`
    - packages/api/src/context-bridge.ts contains `CONTEXT.md`
    - packages/api/src/context-bridge.ts contains `extractRequirementIds`
    - packages/api/src/commands/interview.ts contains `InterviewFSM`
    - packages/api/src/commands/interview.ts contains `startOrResume`
    - packages/api/src/commands/interview.ts contains `submitAnswer`
    - packages/api/src/commands/interview.ts contains `writeSeedDraft`
    - packages/api/src/commands/interview.ts contains `readPlanningArtifacts`
    - packages/api/src/__tests__/context-bridge.test.ts exists
    - `pnpm --filter @cauldron/api test` exits 0
  </acceptance_criteria>
  <done>Context bridge reads .planning/ artifacts with requirement ID extraction. Interview command drives FSM with prior context injection and outputs seed draft file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crystallize command for file-based seed approval</name>
  <files>
    packages/api/src/commands/crystallize.ts,
    packages/api/src/__tests__/crystallize.test.ts
  </files>
  <read_first>
    packages/engine/src/interview/fsm.ts,
    packages/engine/src/interview/crystallizer.ts,
    packages/api/src/review/seed-writer.ts,
    packages/api/src/commands/interview.ts
  </read_first>
  <behavior>
    - Test 1: crystallizeCommand reads seed draft file via readSeedDraft
    - Test 2: crystallizeCommand calls fsm.approveAndCrystallize with the interview/project IDs
    - Test 3: crystallizeCommand exits with error when seed draft file does not exist
    - Test 4: crystallizeCommand prints the crystallized seed ID on success
  </behavior>
  <action>
    1. Replace `packages/api/src/commands/crystallize.ts` stub (per D-16):
       - Export `async function crystallizeCommand(deps: BootstrapDeps, args: string[]): Promise<void>`
       - Parse args: `--project-id` (required), `--project-root` (defaults to cwd)
       - Read the seed draft: `const summary = await readSeedDraft(projectRoot, projectId)` — if file not found, print "No seed draft found. Run /cauldron:interview first." and exit 1
       - Look up the most recent interview for this project: query `interviews` table where `projectId` matches and status is `reviewing`, ordered by `createdAt` desc, take first
       - If no interview found in reviewing state, print "No interview in reviewing state. Run /cauldron:interview first." and exit 1
       - Create FSM: `const fsm = new InterviewFSM(deps.db, deps.gateway, deps.config, deps.logger)`
       - Call `const seed = await fsm.approveAndCrystallize(interview.id, projectId)`
       - Print: `console.log('Seed crystallized: ' + seed.id)`
       - Clean up: delete the draft file (optional — or leave it as a record)
       - Exit 0

    2. Create `packages/api/src/__tests__/crystallize.test.ts`:
       - Mock InterviewFSM, mock readSeedDraft, mock db queries
       - Test happy path: draft exists, interview in reviewing state, crystallize succeeds
       - Test error: no draft file
       - Test error: no interview in reviewing state

    All files use `.js` extensions on relative imports.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @cauldron/api test && pnpm --filter @cauldron/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/commands/crystallize.ts contains `readSeedDraft`
    - packages/api/src/commands/crystallize.ts contains `approveAndCrystallize`
    - packages/api/src/commands/crystallize.ts contains `InterviewFSM`
    - packages/api/src/__tests__/crystallize.test.ts exists and contains `describe`
    - `pnpm --filter @cauldron/api test` exits 0
  </acceptance_criteria>
  <done>Crystallize command reads seed draft file, finds reviewing interview, calls approveAndCrystallize, prints seed ID. Tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cauldron/api test` — all unit tests pass
- `pnpm --filter @cauldron/api typecheck` — no type errors
</verification>

<success_criteria>
- Context bridge reads PROJECT.md, REQUIREMENTS.md, phase CONTEXT.md and concatenates them
- Requirement IDs are extractable for D-10 traceability
- Interview command drives FSM with GSD prior context
- Interview outputs seed draft JSON file for review
- Crystallize reads draft and finalizes crystallization
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-03-SUMMARY.md`
</output>
