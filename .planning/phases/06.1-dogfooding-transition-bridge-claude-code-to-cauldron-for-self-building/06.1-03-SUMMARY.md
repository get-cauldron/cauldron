---
phase: "06.1"
plan: "03"
subsystem: "api-cli"
tags: ["interview", "crystallize", "context-bridge", "gsd-bridging", "seed-review", "fsm"]
dependency_graph:
  requires:
    - "InterviewFSM from packages/engine/src/interview/fsm.ts"
    - "writeSeedDraft/readSeedDraft from packages/api/src/review/seed-writer.ts"
    - "bootstrap from packages/api/src/bootstrap.ts"
  provides:
    - "readPlanningArtifacts: reads .planning/ artifacts for GSD context bridging"
    - "extractRequirementIds: extracts [A-Z]+-\\d+ IDs for D-10 traceability"
    - "interviewCommand: drives InterviewFSM with prior context injection, outputs seed draft"
    - "crystallizeCommand: reads seed draft and calls approveAndCrystallize"
  affects:
    - "packages/api/src/cli.ts (interview and crystallize routes now fully implemented)"
    - "packages/engine (InterviewFSM consumed via CLI for the first time)"
tech_stack:
  added: []
  patterns:
    - "Commands self-contained: internally call bootstrap() + parseArgs() — no args passed from cli.ts"
    - "GSD bridging: priorContext prepended to first user answer as brownfield preamble"
    - "parseArgs with strict:false returns string|boolean — requires explicit type assertions"
    - "process.exit(1) followed by return to prevent execution continuation in test environments"
    - "TDD: RED commit (failing tests) then GREEN commit (implementation) for each task"
key_files:
  created:
    - "packages/api/src/context-bridge.ts"
    - "packages/api/src/__tests__/context-bridge.test.ts"
    - "packages/api/src/__tests__/interview-command.test.ts"
    - "packages/api/src/__tests__/crystallize.test.ts"
  modified:
    - "packages/api/src/commands/interview.ts"
    - "packages/api/src/commands/crystallize.ts"
decisions:
  - "Commands self-contained (bootstrap + parseArgs internally): cli.ts calls them with no args, keeping the routing simple"
  - "Prior context injected as first-answer preamble: safest approach that works with FSM scoring without DB surgery"
  - "Brownfield mode auto-selected when priorContext is non-empty: ties D-07 to D-04 naturally"
  - "process.exit(1) + return pattern: required for test environments where process.exit is mocked"
metrics:
  duration: "7 min"
  completed: "2026-03-26"
  tasks: 2
  files: 6
---

# Phase 6.1 Plan 03: Interview Command with GSD Context Bridging Summary

Interview command drives InterviewFSM with GSD prior context injection (brownfield mode), and crystallize command finalizes seeds from reviewed JSON draft files.

## Completed Tasks

### Task 1: GSD context bridge and interview command

**RED:** Created 2 failing test files — `context-bridge.test.ts` (5 tests for `readPlanningArtifacts` and `extractRequirementIds`) and `interview-command.test.ts` (2 tests for FSM wiring and prior context injection).

**GREEN:** Implemented:
- `context-bridge.ts`: `readPlanningArtifacts` reads `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, and phase-specific CONTEXT.md files. Returns empty string when `.planning/` doesn't exist. `extractRequirementIds` extracts `[A-Z]+-\d+` patterns (EVOL-01, D-07, etc.) for D-10 traceability.
- `commands/interview.ts`: Replaced stub with full implementation. Uses `parseArgs` to parse `--project-id`, `--project-root`, `--phase`. Calls `readPlanningArtifacts` to get prior context, selects `brownfield` mode when context exists. Creates `InterviewFSM`, calls `startOrResume`, then enters a readline turn loop. On threshold met (`thresholdMet: true`), calls `generateSummary` and `writeSeedDraft`. Prints draft path and next command.

Note: `seed-writer.ts`, `holdout-writer.ts`, `kill.ts`, and `status.ts` were simultaneously implemented by a parallel Plan 02 execution. This plan only re-staged `context-bridge.ts` and `interview.ts` in its commit.

**Commits:**
- `test(06.1-03): add failing tests for context bridge and interview command` (c42a7f0)
- `feat(06.1-03): context bridge, interview command, and review writers` (75a5f4e)

### Task 2: Crystallize command for file-based seed approval

**RED:** Created `crystallize.test.ts` with 4 failing tests: `readSeedDraft` called, `approveAndCrystallize` called with correct args, error on missing draft, seed ID printed on success.

**GREEN:** Implemented `commands/crystallize.ts`:
- Parses `--project-id` and `--project-root` via `parseArgs`
- Calls `readSeedDraft(projectRoot, projectId)` — exits with error if file not found
- Queries `interviews` table for most recent row, checks `phase === 'reviewing'`
- Creates `InterviewFSM` and calls `approveAndCrystallize(interview.id, projectId, summary)`
- Prints `Seed crystallized: {id}` on success

**Commits:**
- `test(06.1-03): add failing tests for crystallize command` (212255b)
- `feat(06.1-03): crystallize command for file-based seed approval` (66d3ec3)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] parseArgs strict:false returns string|boolean union type**
- **Found during:** Task 1 GREEN typecheck
- **Issue:** `parseArgs` with `strict: false` returns `string | boolean` for all option values, causing TS2345 errors when passing to functions expecting `string`
- **Fix:** Added explicit `as string | undefined` type assertions for all `values[...]` accesses
- **Files modified:** `packages/api/src/commands/interview.ts`
- **Commit:** 75a5f4e

**2. [Rule 3 - Blocking] process.exit(1) without return causes test continuation failure**
- **Found during:** Task 2 GREEN phase
- **Issue:** In Test 3, `process.exit` is mocked (doesn't actually exit), so code after `process.exit(1)` continues executing and crashes on undefined `db.select()`
- **Fix:** Added `return` after each `process.exit(1)` call in crystallize command
- **Files modified:** `packages/api/src/commands/crystallize.ts`
- **Commit:** 66d3ec3

**3. [Rule 3 - Blocking] context-bridge.ts JSDoc glob pattern caused Vite parse error**
- **Found during:** Task 1 first test run
- **Issue:** JSDoc comment containing `phases/06.1-*/06.1-CONTEXT.md` was parsed as a glob by the Vite/OXC transformer, causing a parse error
- **Fix:** Rewrote the JSDoc comment to avoid the glob pattern in the source
- **Files modified:** `packages/api/src/context-bridge.ts`
- **Commit:** 75a5f4e

**4. [Rule 3 - Blocking] context-bridge returned empty when only REQUIREMENTS.md present**
- **Found during:** Task 1 RED→GREEN
- **Issue:** Test 3 creates only REQUIREMENTS.md (no PROJECT.md), but original code returned empty if PROJECT.md was missing
- **Fix:** Restructured to check `.planning/` directory existence first, then read each file independently
- **Files modified:** `packages/api/src/context-bridge.ts`
- **Commit:** 75a5f4e

### Plan Scope Adjustment

The plan specified that `interviewCommand(deps: BootstrapDeps, args: string[]): Promise<void>` — but `cli.ts` calls commands with no args. The commands were implemented as self-contained `(): Promise<void>` functions that call `bootstrap()` and `parseArgs()` internally. This matches the existing cli.ts calling convention without requiring cli.ts changes, which was not listed as a file to modify.

## Known Stubs

None — interview and crystallize commands are fully implemented.

## Self-Check: PASSED

Verification results:

- `/Users/zakkeown/Code/cauldron/packages/api/src/context-bridge.ts` — FOUND
- `/Users/zakkeown/Code/cauldron/packages/api/src/commands/interview.ts` — FOUND (contains InterviewFSM, startOrResume, submitAnswer, writeSeedDraft, readPlanningArtifacts)
- `/Users/zakkeown/Code/cauldron/packages/api/src/commands/crystallize.ts` — FOUND (contains readSeedDraft, approveAndCrystallize, InterviewFSM)
- `/Users/zakkeown/Code/cauldron/packages/api/src/__tests__/context-bridge.test.ts` — FOUND
- `/Users/zakkeown/Code/cauldron/packages/api/src/__tests__/interview-command.test.ts` — FOUND
- `/Users/zakkeown/Code/cauldron/packages/api/src/__tests__/crystallize.test.ts` — FOUND
- All 25 tests pass, typecheck clean
- Commits c42a7f0, 75a5f4e, 212255b, 66d3ec3 verified in git log
