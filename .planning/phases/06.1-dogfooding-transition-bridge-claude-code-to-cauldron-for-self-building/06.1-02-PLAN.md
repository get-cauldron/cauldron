---
phase: 06.1-dogfooding-transition
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - packages/api/src/commands/status.ts
  - packages/api/src/commands/kill.ts
  - packages/api/src/review/seed-writer.ts
  - packages/api/src/review/holdout-writer.ts
  - packages/api/src/__tests__/status.test.ts
  - packages/api/src/__tests__/review-writer.test.ts
autonomous: true
requirements:
  - D-04
  - D-16
  - D-17
  - D-18
  - D-20

must_haves:
  truths:
    - "Status command displays a table of beads with name, status, agent, duration columns"
    - "Status command with --logs flag tails recent events"
    - "Kill command marks a bead as failed and logs the event"
    - "Seed review writer outputs valid YAML with all SeedSummary fields"
    - "Holdout review writer outputs YAML with per-scenario approved field"
  artifacts:
    - path: "packages/api/src/commands/status.ts"
      provides: "Bead status table query and rendering"
      contains: "console.table"
    - path: "packages/api/src/commands/kill.ts"
      provides: "Bead abort command"
      contains: "bead_failed"
    - path: "packages/api/src/review/seed-writer.ts"
      provides: "Seed YAML draft writer for file-based review"
      exports: ["writeSeedDraft"]
    - path: "packages/api/src/review/holdout-writer.ts"
      provides: "Holdout scenarios YAML writer for file-based review"
      exports: ["writeHoldoutDraft"]
  key_links:
    - from: "packages/api/src/commands/status.ts"
      to: "packages/shared/src/db/schema/bead.ts"
      via: "drizzle query on beads table"
      pattern: "from.*beads"
    - from: "packages/api/src/commands/kill.ts"
      to: "packages/shared/src/db/event-store.ts"
      via: "appendEvent for bead_failed"
      pattern: "appendEvent"
---

<objective>
Implement the read-only and simple commands (status, kill) plus the file-based review writers used by interview and seal commands.

Purpose: Status is the primary observability tool (D-18). Kill enables stuck-bead intervention (D-20). Review writers are shared infrastructure for seed review (D-16) and holdout review (D-17).
Output: Working status/kill commands, reusable seed and holdout review file writers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-CONTEXT.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-RESEARCH.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-01-SUMMARY.md

<interfaces>
<!-- From packages/shared/src/db/schema/bead.ts -->
```typescript
export const beadStatusEnum = pgEnum('bead_status', ['pending', 'claimed', 'active', 'completed', 'failed']);
export const beads = pgTable('beads', {
  id: uuid('id').primaryKey().defaultRandom(),
  seedId: uuid('seed_id').notNull(),
  moleculeId: uuid('molecule_id'),
  title: text('title').notNull(),
  spec: text('spec').notNull(),
  status: beadStatusEnum('status').notNull().default('pending'),
  estimatedTokens: integer('estimated_tokens'),
  agentAssignment: text('agent_assignment'),
  claimedAt: timestamp('claimed_at', { withTimezone: true }),
  completedAt: timestamp('completed_at', { withTimezone: true }),
  version: integer('version').notNull().default(1),
});
```

<!-- From packages/shared/src/db/event-store.ts -->
```typescript
export function appendEvent(db: DbClient, event: { projectId: string; type: string; payload: unknown }): Promise<void>;
```

<!-- From packages/engine/src/interview/types.ts -->
```typescript
export interface SeedSummary {
  goal: string;
  constraints: string[];
  acceptanceCriteria: string[];
  ontologySchema: OntologySchema;
  evaluationPrinciples: string[];
  exitConditions: string[];
}
```

<!-- From packages/engine/src/holdout/types.ts -->
```typescript
export interface HoldoutScenario {
  id: string;
  title: string;
  description: string;
  testCode: string;
  category: string;
}
```

<!-- From packages/api/src/bootstrap.ts (created in Plan 01) -->
```typescript
export interface BootstrapDeps {
  db: DbClient;
  gateway: LLMGateway;
  inngest: Inngest;
  logger: Logger;
  config: GatewayConfig;
}
export async function bootstrap(projectRoot: string): Promise<BootstrapDeps>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status command and kill command</name>
  <files>
    packages/api/src/commands/status.ts,
    packages/api/src/commands/kill.ts,
    packages/api/src/__tests__/status.test.ts
  </files>
  <read_first>
    packages/shared/src/db/schema/bead.ts,
    packages/shared/src/db/schema/event.ts,
    packages/shared/src/db/event-store.ts,
    packages/api/src/cli.ts,
    packages/api/src/bootstrap.ts
  </read_first>
  <behavior>
    - Test 1: statusCommand queries beads for a given seedId and renders a table with title, status, agent, duration columns
    - Test 2: statusCommand with --logs flag queries events table and prints recent events (last 20)
    - Test 3: statusCommand shows "NEEDS REVIEW" for beads with merge_escalation_needed events
    - Test 4: killCommand sets bead status to 'failed' and appends a bead_failed event
    - Test 5: killCommand exits with error when bead ID not found
  </behavior>
  <action>
    1. Replace `packages/api/src/commands/status.ts` stub with full implementation (per D-04, D-18):
       - Export `async function statusCommand(deps: BootstrapDeps, args: string[]): Promise<void>`
       - Parse args: first positional is `seedId` (required). `--logs` flag enables event tailing.
       - Query beads table: `deps.db.select({ title: beads.title, status: beads.status, agent: beads.agentAssignment, claimedAt: beads.claimedAt, completedAt: beads.completedAt }).from(beads).where(eq(beads.seedId, seedId)).orderBy(beads.createdAt)`
       - Compute duration for each row: `completedAt ? completedAt - claimedAt : claimedAt ? Date.now() - claimedAt : null`, format as "Xm Ys"
       - Check events table for `merge_escalation_needed` events per beadId — if found, override status display to "NEEDS REVIEW" (per D-19)
       - Render with `console.table()` — columns: Title, Status, Agent, Duration
       - Print summary line: "X completed, Y active, Z pending, W failed"
       - If `--logs` flag: query events table for last 20 events ordered by `createdAt` desc, print each as `[timestamp] type: payload_summary`
       - If no seedId provided, query `seeds` table for the most recent seed and use its ID

    2. Replace `packages/api/src/commands/kill.ts` stub with full implementation (per D-20):
       - Export `async function killCommand(deps: BootstrapDeps, args: string[]): Promise<void>`
       - Parse args: first positional is `beadId` (required)
       - Verify bead exists: `deps.db.select().from(beads).where(eq(beads.id, beadId))`
       - If not found, `console.error('Bead not found: ' + beadId)` and `process.exit(1)`
       - Update bead status to 'failed': `deps.db.update(beads).set({ status: 'failed' }).where(eq(beads.id, beadId))`
       - Append event: `appendEvent(deps.db, { projectId, type: 'bead_failed', payload: { beadId, reason: 'manual_kill' } })`
       - Print confirmation: `console.log('Killed bead: ' + beadId)`

    3. Create `packages/api/src/__tests__/status.test.ts`:
       - Mock `@cauldron/shared` (db, beads, events, seeds tables)
       - Test status table rendering, --logs flag, NEEDS REVIEW detection
       - Test kill bead-not-found and success paths

    All files use `.js` extensions on relative imports.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @cauldron/api test && pnpm --filter @cauldron/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/commands/status.ts contains `console.table`
    - packages/api/src/commands/status.ts contains `merge_escalation_needed` string for NEEDS REVIEW detection
    - packages/api/src/commands/status.ts contains `--logs` handling
    - packages/api/src/commands/kill.ts contains `appendEvent`
    - packages/api/src/commands/kill.ts contains `status: 'failed'`
    - packages/api/src/__tests__/status.test.ts contains `describe`
    - `pnpm --filter @cauldron/api test` exits 0
  </acceptance_criteria>
  <done>Status command renders bead table with duration, supports --logs, shows NEEDS REVIEW. Kill command aborts bead and logs event. Unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: File-based review writers for seed and holdout</name>
  <files>
    packages/api/src/review/seed-writer.ts,
    packages/api/src/review/holdout-writer.ts,
    packages/api/src/__tests__/review-writer.test.ts
  </files>
  <read_first>
    packages/engine/src/interview/types.ts,
    packages/engine/src/holdout/types.ts,
    packages/engine/src/holdout/vault.ts
  </read_first>
  <behavior>
    - Test 1: writeSeedDraft writes a YAML file at .cauldron/review/seed-draft-{projectId}.yaml containing goal, constraints, acceptanceCriteria, ontologySchema, evaluationPrinciples, exitConditions
    - Test 2: writeSeedDraft creates the .cauldron/review/ directory if it does not exist
    - Test 3: readSeedDraft parses the YAML file back into a SeedSummary-shaped object
    - Test 4: writeHoldoutDraft writes a YAML file at .cauldron/review/holdout-draft-{seedId}.yaml with each scenario having id, title, description, testCode, category, and approved: true default
    - Test 5: readHoldoutDraft parses the YAML file and returns array of scenarios with their approved boolean
  </behavior>
  <action>
    1. Create `packages/api/src/review/seed-writer.ts` (per D-16):
       - Export `async function writeSeedDraft(projectRoot: string, projectId: string, summary: SeedSummary): Promise<string>` — returns the file path
       - Write to `{projectRoot}/.cauldron/review/seed-draft-{projectId}.yaml`
       - Use `JSON.stringify` with 2-space indent to write the summary as a human-readable YAML-like JSON file (since Node.js has no built-in YAML serializer and research confirms no external YAML dep needed). Use `.yaml` extension but JSON content — clearly comment at top: `// Edit this file, then run: pnpm exec tsx packages/api/src/cli.ts crystallize`
       - Actually, use a simple hand-rolled YAML serialization for readability: write each field on its own line with proper YAML formatting. For arrays, use `- item` format. For nested objects, use indentation. Keep it simple — SeedSummary has a flat-ish structure.
       - Create directory with `fs.mkdirSync(dir, { recursive: true })`
       - Export `async function readSeedDraft(projectRoot: string, projectId: string): Promise<SeedSummary>` — reads and parses the file back. Use a simple YAML parser (key: value lines, `- item` arrays). Or just use JSON format for round-trip safety — write as pretty JSON with `.yaml` extension and a comment header explaining it's JSON for reliable parsing.

       **Decision:** Use JSON format with `.json` extension for reliable round-tripping. The file header includes a comment explaining what to edit and the next command to run. This avoids hand-rolling a YAML parser.
       - File path: `{projectRoot}/.cauldron/review/seed-draft-{projectId}.json`
       - Write: `JSON.stringify(summary, null, 2)`
       - Read: `JSON.parse(fs.readFileSync(path, 'utf-8'))`

    2. Create `packages/api/src/review/holdout-writer.ts` (per D-17):
       - Export `async function writeHoldoutDraft(projectRoot: string, seedId: string, scenarios: HoldoutScenario[]): Promise<string>`
       - Write to `{projectRoot}/.cauldron/review/holdout-draft-{seedId}.json`
       - Each scenario gets an `approved: true` field added (default approve all — user removes/sets false for rejected ones)
       - Export `async function readHoldoutDraft(projectRoot: string, seedId: string): Promise<Array<HoldoutScenario & { approved: boolean }>>`
       - Read and parse the JSON file, return scenarios with their approved status

    3. Create `packages/api/src/__tests__/review-writer.test.ts`:
       - Use `os.tmpdir()` + random suffix for isolated test directories
       - Test write + read round-trip for seed draft
       - Test write + read round-trip for holdout draft
       - Test directory auto-creation
       - Test that holdout draft defaults all scenarios to approved: true
       - Clean up temp dirs in afterEach

    All files use `.js` extensions on relative imports. Import types from `@cauldron/engine`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @cauldron/api test && pnpm --filter @cauldron/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api/src/review/seed-writer.ts contains `export async function writeSeedDraft`
    - packages/api/src/review/seed-writer.ts contains `export async function readSeedDraft`
    - packages/api/src/review/seed-writer.ts contains `JSON.stringify`
    - packages/api/src/review/holdout-writer.ts contains `export async function writeHoldoutDraft`
    - packages/api/src/review/holdout-writer.ts contains `approved: true`
    - packages/api/src/__tests__/review-writer.test.ts exists and contains `writeSeedDraft`
    - `pnpm --filter @cauldron/api test` exits 0
  </acceptance_criteria>
  <done>Seed and holdout review writers produce JSON files in .cauldron/review/ with round-trip read/write. All unit tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cauldron/api test` — all unit tests pass including status, kill, and review writer tests
- `pnpm --filter @cauldron/api typecheck` — no type errors
</verification>

<success_criteria>
- Status command queries beads and renders table with console.table()
- Status --logs tails events table
- Kill command marks bead failed with event
- Seed writer produces reviewable JSON file with SeedSummary structure
- Holdout writer produces reviewable JSON file with per-scenario approved field
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-02-SUMMARY.md`
</output>
