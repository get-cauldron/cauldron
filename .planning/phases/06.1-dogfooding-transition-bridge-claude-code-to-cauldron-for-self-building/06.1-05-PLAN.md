---
phase: 06.1-dogfooding-transition
plan: 05
type: execute
wave: 4
depends_on: ["06.1-02", "06.1-03", "06.1-04"]
files_modified:
  - .claude/skills/cauldron-interview.md
  - .claude/skills/cauldron-crystallize.md
  - .claude/skills/cauldron-decompose.md
  - .claude/skills/cauldron-execute.md
  - .claude/skills/cauldron-status.md
  - .claude/skills/cauldron-kill.md
  - .claude/skills/cauldron-seal.md
  - .claude/skills/cauldron-resolve.md
autonomous: false
requirements:
  - D-01
  - D-02
  - D-05
  - D-09
  - D-11

must_haves:
  truths:
    - "Each of the 8 skills invokes the CLI via pnpm exec tsx packages/api/src/cli.ts"
    - "Skills include health check step before running the command"
    - "Skills describe what the command does and what to run next"
    - "Dry run validates the full pipeline on the v1 test case (CLI rename tool)"
  artifacts:
    - path: ".claude/skills/cauldron-interview.md"
      provides: "/cauldron:interview skill"
      contains: "cli.ts interview"
    - path: ".claude/skills/cauldron-execute.md"
      provides: "/cauldron:execute skill"
      contains: "cli.ts execute"
    - path: ".claude/skills/cauldron-status.md"
      provides: "/cauldron:status skill"
      contains: "cli.ts status"
    - path: ".claude/skills/cauldron-seal.md"
      provides: "/cauldron:seal skill"
      contains: "cli.ts seal"
  key_links:
    - from: ".claude/skills/cauldron-*.md"
      to: "packages/api/src/cli.ts"
      via: "pnpm exec tsx invocation"
      pattern: "pnpm exec tsx packages/api/src/cli.ts"
---

<objective>
Create all 8 Claude Code skill files and validate the full pipeline with a dry-run checkpoint.

Purpose: Skills are the invocation surface for Claude Code to drive Cauldron (D-01). The dry-run checkpoint (D-09) proves the pipeline works end-to-end before self-build on Phases 7-9.
Output: 8 skill files in .claude/skills/, verified pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-CONTEXT.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-RESEARCH.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-01-SUMMARY.md
@.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 8 Claude Code skill files</name>
  <files>
    .claude/skills/cauldron-interview.md,
    .claude/skills/cauldron-crystallize.md,
    .claude/skills/cauldron-decompose.md,
    .claude/skills/cauldron-execute.md,
    .claude/skills/cauldron-status.md,
    .claude/skills/cauldron-kill.md,
    .claude/skills/cauldron-seal.md,
    .claude/skills/cauldron-resolve.md
  </files>
  <read_first>
    packages/api/src/cli.ts,
    packages/api/src/commands/interview.ts,
    packages/api/src/commands/crystallize.ts,
    packages/api/src/commands/decompose.ts,
    packages/api/src/commands/execute.ts,
    packages/api/src/commands/status.ts,
    packages/api/src/commands/kill.ts,
    packages/api/src/commands/seal.ts,
    packages/api/src/commands/resolve.ts
  </read_first>
  <action>
    Create `.claude/skills/` directory if it does not exist.

    Each skill file follows the same structure (per D-01, D-02, D-05, D-11):
    1. Description of what the command does
    2. Pre-flight: run `pnpm exec tsx packages/api/src/cli.ts health` to verify services. If it fails, run `docker compose up -d` and retry.
    3. CLI invocation: `pnpm exec tsx packages/api/src/cli.ts <command> [args]`
    4. Post-flight: what to expect, what command to run next

    Create these 8 files with the content specified below. Each file is plain markdown that Claude Code reads when the skill is invoked.

    **`.claude/skills/cauldron-status.md`:**
    ```
    # /cauldron:status

    Show the current state of all beads for a seed.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Run: `pnpm exec tsx packages/api/src/cli.ts status --seed-id <SEED_ID>`
       - Add `--logs` to tail recent events
    3. Review the bead table: Title, Status, Agent, Duration
       - "NEEDS REVIEW" means a merge conflict needs resolution via /cauldron:resolve

    ## Next Steps
    - If beads are failed: investigate with /cauldron:status --logs, then /cauldron:kill or /cauldron:execute --resume
    - If NEEDS REVIEW: use /cauldron:resolve <bead-id>
    ```

    **`.claude/skills/cauldron-interview.md`:**
    ```
    # /cauldron:interview

    Start or resume a Socratic interview for a project. Reads .planning/ artifacts as prior context to avoid re-asking decided questions.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Run: `pnpm exec tsx packages/api/src/cli.ts interview --project-id <PROJECT_ID> --project-root <PATH>`
       - Add `--phase <PHASE_ID>` to scope context reading to a specific phase
    3. Answer questions interactively until the ambiguity score reaches <= 0.2
    4. When the interview reaches the reviewing phase, a seed draft is written to .cauldron/review/

    ## Next Steps
    - Review the seed draft file in .cauldron/review/seed-draft-{projectId}.json
    - Edit if needed, then run /cauldron:crystallize
    ```

    **`.claude/skills/cauldron-crystallize.md`:**
    ```
    # /cauldron:crystallize

    Finalize a seed from the reviewed draft file. The seed becomes immutable after crystallization.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Ensure a seed draft exists at .cauldron/review/seed-draft-{projectId}.json (created by /cauldron:interview)
    3. Run: `pnpm exec tsx packages/api/src/cli.ts crystallize --project-id <PROJECT_ID>`
    4. On success, prints the crystallized seed ID

    ## Next Steps
    - Run /cauldron:seal --seed-id <SEED_ID> --generate to create holdout tests
    - Or skip holdouts and go directly to /cauldron:decompose --project-id <PROJECT_ID>
    ```

    **`.claude/skills/cauldron-decompose.md`:**
    ```
    # /cauldron:decompose

    Decompose a crystallized seed into a molecule/bead DAG for parallel execution.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Run: `pnpm exec tsx packages/api/src/cli.ts decompose --project-id <PROJECT_ID>`
       - Optionally: `--seed-id <SEED_ID>` to decompose a specific seed
    3. Prints the number of beads created

    ## Next Steps
    - Run /cauldron:execute --project-id <PROJECT_ID> to start bead execution
    - Run /cauldron:status to view the bead DAG
    ```

    **`.claude/skills/cauldron-execute.md`:**
    ```
    # /cauldron:execute

    Start the Inngest execution engine. Dispatches ready beads for parallel agent execution. This is a long-running process.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Run: `pnpm exec tsx packages/api/src/cli.ts execute --project-id <PROJECT_ID>`
       - Add `--resume` to re-dispatch failed/pending beads without restarting from scratch
       - Add `--seed-id <SEED_ID>` to target a specific seed
    3. The process stays alive while beads execute. Monitor with /cauldron:status in another terminal.
    4. Ctrl-C to stop.

    ## Next Steps
    - Monitor progress: /cauldron:status --seed-id <SEED_ID>
    - If a bead is stuck: /cauldron:kill <bead-id>
    - If execution was interrupted: /cauldron:execute --resume
    ```

    **`.claude/skills/cauldron-kill.md`:**
    ```
    # /cauldron:kill

    Abort a stuck or misbehaving bead. Marks it as failed.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Run: `pnpm exec tsx packages/api/src/cli.ts kill <BEAD_ID>`
    3. Confirms the bead was marked as failed

    ## Next Steps
    - Run /cauldron:execute --resume to re-dispatch remaining beads
    - Run /cauldron:status to see updated bead states
    ```

    **`.claude/skills/cauldron-seal.md`:**
    ```
    # /cauldron:seal

    Generate, review, and seal holdout tests. Two-step process: generate scenarios, then seal after review.

    ## Usage

    ### Step 1: Generate holdout scenarios
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
    2. Run: `pnpm exec tsx packages/api/src/cli.ts seal --seed-id <SEED_ID> --generate`
    3. Review the holdout draft at .cauldron/review/holdout-draft-{seedId}.json
    4. Set `approved: false` on any scenarios to reject

    ### Step 2: Seal the vault
    1. Run: `pnpm exec tsx packages/api/src/cli.ts seal --seed-id <SEED_ID>`
    2. Approved scenarios are encrypted and sealed

    ## Next Steps
    - Run /cauldron:decompose --project-id <PROJECT_ID> to decompose the seed
    ```

    **`.claude/skills/cauldron-resolve.md`:**
    ```
    # /cauldron:resolve

    Resolve a merge conflict for a bead. The conflict file is at .cauldron/review/conflict-{beadId}.diff.

    ## Usage
    1. Health check: `pnpm exec tsx packages/api/src/cli.ts health`
       - If this fails, run `docker compose up -d` and retry
    2. Find the conflict file: .cauldron/review/conflict-{beadId}.diff
    3. Edit the file to resolve conflicts
    4. Run: `pnpm exec tsx packages/api/src/cli.ts resolve <BEAD_ID>`
    5. The bead will be re-queued for merge

    ## Next Steps
    - Run /cauldron:status to verify the bead is back in the merge queue
    ```
  </action>
  <verify>
    <automated>ls -la /Users/zakkeown/Code/cauldron/.claude/skills/cauldron-*.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/cauldron-interview.md exists and contains `cli.ts interview`
    - .claude/skills/cauldron-crystallize.md exists and contains `cli.ts crystallize`
    - .claude/skills/cauldron-decompose.md exists and contains `cli.ts decompose`
    - .claude/skills/cauldron-execute.md exists and contains `cli.ts execute`
    - .claude/skills/cauldron-status.md exists and contains `cli.ts status`
    - .claude/skills/cauldron-kill.md exists and contains `cli.ts kill`
    - .claude/skills/cauldron-seal.md exists and contains `cli.ts seal`
    - .claude/skills/cauldron-resolve.md exists and contains `cli.ts resolve`
    - All 8 skills contain `pnpm exec tsx packages/api/src/cli.ts health` pre-flight
    - `ls .claude/skills/cauldron-*.md | wc -l` outputs 8
  </acceptance_criteria>
  <done>All 8 Claude Code skill files created with health check, CLI invocation, and next-steps guidance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dry-run pipeline validation</name>
  <files>none</files>
  <action>
    Human verifies the full pipeline works end-to-end. The executor runs the automated checks below before presenting the checkpoint to the user.
    1. Run `docker compose up -d` to ensure all services are started
    2. Run `pnpm exec tsx packages/api/src/cli.ts health` to verify DB + Inngest connectivity
    3. Run `pnpm typecheck` to verify all packages compile
    4. Run `pnpm --filter @cauldron/api test` to verify all API unit tests pass
    5. Run `pnpm --filter @cauldron/engine test` to verify no engine regressions
    6. Run `pnpm build` to verify build succeeds
    7. Run `ls .claude/skills/cauldron-*.md` to verify all 8 skill files exist
    8. Run `grep selfBuild cauldron.config.ts` to verify selfBuild flag is present
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm typecheck && pnpm --filter @cauldron/api test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm --filter @cauldron/api test` exits 0
    - `pnpm build` exits 0
    - 8 skill files exist in .claude/skills/
    - cauldron.config.ts contains `selfBuild: true`
  </acceptance_criteria>
  <done>Human has verified: health check works, all tests pass, build succeeds, skills exist, CLI routes all commands</done>
</task>

</tasks>

<verification>
- All 8 skill files exist in .claude/skills/
- Full regression suite passes: `pnpm test && pnpm typecheck && pnpm build`
- Health check command works against running Docker services
- CLI routes all 9 subcommands without errors
</verification>

<success_criteria>
- 8 Claude Code skills created, each invoking CLI via pnpm exec tsx
- Skills include health check pre-flight
- Full pipeline is functional (verified by human)
- Build, typecheck, and tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-05-SUMMARY.md`
</output>
