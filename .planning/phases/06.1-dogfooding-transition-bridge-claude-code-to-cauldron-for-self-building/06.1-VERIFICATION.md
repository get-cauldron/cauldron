---
phase: 06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building
verified: 2026-03-26T18:55:51Z
status: passed
score: 21/21 requirements satisfied (D-09 validated via manual dry-run with findings deferred to 6.2)
gaps:
  - truth: "Dry-run validates the full pipeline on the v1 test case (CLI rename tool) in a separate repo"
    status: validated-with-findings
    reason: "Dry-run performed manually against external CLI renamer repo. Plumbing validated: services connect, migrations auto-apply, gateway routes to available providers, interview produces context-aware questions. Interview engine convergence issues (scorer calibration, perspective rotation, convergence pressure) prevent reaching crystallization. Deferred to Phase 6.2."
    artifacts:
      - path: ".planning/phases/06.1-dogfooding-transition-bridge-claude-code-to-cauldron-for-self-building/06.1-05-SUMMARY.md"
        issue: "Dry-run validation documented as 'pnpm typecheck + tests + build' passing, not an actual pipeline run on external software"
    missing:
      - "Create a minimal CLI file-renaming fixture repo (separate git repo with package.json, src/, cauldron.config.ts with selfBuild: false)"
      - "Run the full pipeline against it: interview → crystallize → seal → decompose → execute (or document explicit deferral decision)"
human_verification:
  - test: "D-09 dry-run pipeline on external project"
    expected: "Full Cauldron pipeline (interview → crystallize → seal → decompose → execute) completes successfully on a separate CLI renaming tool project. Skills work end-to-end against a non-Cauldron codebase."
    why_human: "Requires running Docker services, an interactive interview session, and observing actual bead dispatch. Cannot verify programmatically without a live environment."
---

# Phase 6.1: Dogfooding Transition Bridge Verification Report

**Phase Goal:** Build the invocation surface and bridging infrastructure that lets Claude Code drive Cauldron's engine to plan and execute subsequent phases (7-9) on Cauldron's own codebase. Delivers: CLI entrypoint in packages/api, project-local Claude Code skills per pipeline stage, GSD-to-Cauldron context bridging, self-build safety mode, and a dry-run validation on the v1 test case.
**Verified:** 2026-03-26T18:55:51Z
**Status:** gaps_found — 20/21 requirements satisfied; D-09 dry-run on external project not completed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CLI entrypoint in packages/api routes all 9 subcommands | VERIFIED | `packages/api/src/cli.ts` routes health, status, interview, crystallize, decompose, execute, kill, seal, resolve via parseArgs |
| 2 | Health check verifies DB + Inngest reachability before all non-health commands | VERIFIED | `health.ts` checks `db.execute(sql\`SELECT 1\`)` and `fetch('http://localhost:8288/v1/events')` with process.exit(1) on failure; cli.ts calls `healthCheck()` for all non-health commands |
| 3 | selfBuild flag is readable from cauldron.config.ts via loadConfig() | VERIFIED | `GatewayConfig.selfBuild?: boolean` in gateway/config.ts; `cauldron.config.ts` sets `selfBuild: true` |
| 4 | GSD context bridge reads .planning/ artifacts and injects them as prior context | VERIFIED | `context-bridge.ts` reads PROJECT.md, REQUIREMENTS.md, phase CONTEXT.md and concatenates with section headers; `extractRequirementIds` extracts [A-Z]+-\d+ patterns |
| 5 | Interview command drives InterviewFSM with prior context and outputs seed draft | VERIFIED | `interview.ts` calls `readPlanningArtifacts`, selects brownfield mode when context present, runs FSM turn loop, calls `generateSummary` + `writeSeedDraft` on threshold met |
| 6 | Crystallize command reads seed draft and calls approveAndCrystallize | VERIFIED | `crystallize.ts` calls `readSeedDraft`, queries interviews for reviewing phase, calls `fsm.approveAndCrystallize` |
| 7 | Status command displays bead table with duration, NEEDS REVIEW detection, --logs flag | VERIFIED | `status.ts` queries beads by seedId, computes duration, checks `merge_escalation_needed` events, calls `console.table`, tails 20 events with --logs |
| 8 | Kill command marks bead failed and appends bead_failed event | VERIFIED | `kill.ts` updates `status: 'failed'`, calls `appendEvent` with `type: 'bead_failed'` |
| 9 | Decompose command calls runDecomposition with seed and project context | VERIFIED | `decompose.ts` queries crystallized seed, calls `runDecomposition` with `tokenBudget: 180_000` |
| 10 | Execute command starts Hono/Inngest HTTP server and dispatches ready beads | VERIFIED | `execute.ts` creates Hono app, mounts inngestServe on /api/inngest port 3001, dispatches via `inngest.send` with `bead.dispatch_requested` |
| 11 | Execute --resume re-dispatches pending/failed beads only | VERIFIED | `execute.ts` resets `failed` beads to `pending` before calling `findReadyBeads` when `--resume` flag present |
| 12 | Seal command handles holdout generation, review file, and vault encryption | VERIFIED | `seal.ts` two-mode: --generate calls `generateHoldoutScenarios` + `createVault` + `writeHoldoutDraft`; seal mode reads draft, filters approved, calls `approveScenarios` + `sealVault` |
| 13 | Resolve command reads conflict diff file and re-queues merge | VERIFIED | `resolve.ts` checks for conflict file, updates bead to `pending`, appends `conflict_resolved` event |
| 14 | Engine snapshot captures git commit hash when selfBuild is true | VERIFIED | `self-build.ts` `captureEngineSnapshot` runs `git rev-parse HEAD`; `detectEngineChange` compares HEAD to snapshot; `hasMigrationFiles` detects migration paths |
| 15 | Migration files in bead diffs trigger human review escalation | VERIFIED | `hasMigrationFiles` and `getBeadDiff` implemented in `self-build.ts`; execute.ts captures snapshot when `selfBuild: true` |
| 16 | File-based seed review writer produces reviewable JSON files | VERIFIED | `seed-writer.ts` writes `SeedSummary` as pretty JSON to `.cauldron/review/seed-draft-{projectId}.json`, reads back with `readSeedDraft` |
| 17 | File-based holdout review writer produces JSON with per-scenario approved field | VERIFIED | `holdout-writer.ts` writes `HoldoutScenario[]` with `approved: true` added to each; `readHoldoutDraft` returns typed array |
| 18 | All 8 Claude Code skills invoke CLI via pnpm exec tsx and include health pre-flight | VERIFIED | All 8 `.claude/skills/cauldron-*.md` files contain `pnpm exec tsx packages/api/src/cli.ts health` and the relevant command invocation |
| 19 | Requirement IDs (EVOL-01 etc.) preserved in seed acceptance criteria via extractRequirementIds | VERIFIED | `extractRequirementIds` regex `[A-Z]+-\d+` in context-bridge.ts; 5 tests passing in context-bridge.test.ts |
| 20 | All 21 D-IDs (D-01 through D-21) claimed by plan frontmatter requirements fields | VERIFIED | All D-01..D-21 appear in requirements fields across Plans 01-05 |
| 21 | Dry-run validates full pipeline on v1 test case (CLI rename tool) in separate repo | PARTIAL | Plan 05 Task 2 checkpoint verified build/test/typecheck only; no separate repo created, no actual end-to-end pipeline executed |

**Score:** 20/21 truths verified (D-09 partial)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/api/src/cli.ts` | CLI entrypoint with 9-command routing | VERIFIED | Contains parseArgs routing for all 9 commands; healthCheck() pre-flight on all non-health commands |
| `packages/api/src/bootstrap.ts` | Dependency construction from env | VERIFIED | Exports `bootstrap()`, constructs db, gateway, inngest, logger, config |
| `packages/api/src/health.ts` | DB + Inngest reachability check | VERIFIED | Checks PostgreSQL via `SELECT 1` and Inngest via `GET /v1/events` |
| `packages/api/src/context-bridge.ts` | GSD artifact reader for .planning/ | VERIFIED | Exports `readPlanningArtifacts` and `extractRequirementIds` |
| `packages/api/src/commands/interview.ts` | Interactive interview command | VERIFIED | Contains InterviewFSM, startOrResume, submitAnswer, writeSeedDraft, readPlanningArtifacts |
| `packages/api/src/commands/crystallize.ts` | Seed crystallization from review file | VERIFIED | Contains readSeedDraft, approveAndCrystallize, InterviewFSM |
| `packages/api/src/commands/status.ts` | Bead status table query and rendering | VERIFIED | Contains console.table, merge_escalation_needed, --logs handling |
| `packages/api/src/commands/kill.ts` | Bead abort command | VERIFIED | Contains appendEvent, `status: 'failed'` |
| `packages/api/src/commands/decompose.ts` | Seed decomposition command | VERIFIED | Contains runDecomposition, tokenBudget |
| `packages/api/src/commands/execute.ts` | Inngest HTTP serve + bead dispatch | VERIFIED | Contains new Hono(), inngestServe, bead.dispatch_requested, findReadyBeads, --resume handling, selfBuild check |
| `packages/api/src/commands/seal.ts` | Holdout review + seal command | VERIFIED | Contains generateHoldoutScenarios, sealVault, HOLDOUT_ENCRYPTION_KEY (via sealVault internals), readHoldoutDraft |
| `packages/api/src/commands/resolve.ts` | Merge conflict resolution command | VERIFIED | Contains conflict_resolved, appendEvent |
| `packages/api/src/self-build.ts` | Engine snapshot + migration gate logic | VERIFIED | Exports captureEngineSnapshot, detectEngineChange, hasMigrationFiles, getBeadDiff; uses `rev-parse HEAD` |
| `packages/api/src/review/seed-writer.ts` | Seed YAML draft writer | VERIFIED | Exports writeSeedDraft, readSeedDraft, uses JSON.stringify |
| `packages/api/src/review/holdout-writer.ts` | Holdout scenarios writer | VERIFIED | Exports writeHoldoutDraft, readHoldoutDraft; adds `approved: true` |
| `packages/engine/src/gateway/config.ts` | GatewayConfig with selfBuild field | VERIFIED | Contains `selfBuild?: boolean` field |
| `cauldron.config.ts` | Project config with selfBuild: true | VERIFIED | Contains `selfBuild: true` |
| `.claude/skills/cauldron-interview.md` | /cauldron:interview skill | VERIFIED | Contains `cli.ts interview` and health check |
| `.claude/skills/cauldron-execute.md` | /cauldron:execute skill | VERIFIED | Contains `cli.ts execute` and health check |
| `.claude/skills/cauldron-status.md` | /cauldron:status skill | VERIFIED | Contains `cli.ts status` and health check |
| `.claude/skills/cauldron-seal.md` | /cauldron:seal skill | VERIFIED | Contains `cli.ts seal` and health check |
| `.claude/skills/cauldron-crystallize.md` | /cauldron:crystallize skill | VERIFIED | Contains `cli.ts crystallize` and health check |
| `.claude/skills/cauldron-decompose.md` | /cauldron:decompose skill | VERIFIED | Contains `cli.ts decompose` and health check |
| `.claude/skills/cauldron-kill.md` | /cauldron:kill skill | VERIFIED | Contains `cli.ts kill` and health check |
| `.claude/skills/cauldron-resolve.md` | /cauldron:resolve skill | VERIFIED | Contains `cli.ts resolve` and health check |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `packages/api/src/cli.ts` | `packages/api/src/bootstrap.ts` | `import bootstrap` | WIRED | Line 3: `import { bootstrap } from './bootstrap.js'` |
| `packages/api/src/bootstrap.ts` | `packages/engine/src/holdout/events.ts` | inngest singleton import | WIRED | `import { inngest, ... } from '@cauldron/engine'`; inngest exported from holdout/events.ts → holdout/index.ts → engine barrel |
| `packages/api/src/commands/interview.ts` | `packages/engine/src/interview/fsm.ts` | InterviewFSM.startOrResume + submitAnswer | WIRED | Direct InterviewFSM import and call; turn loop calls submitAnswer |
| `packages/api/src/context-bridge.ts` | `.planning/` | fs.readFileSync of PROJECT.md, REQUIREMENTS.md, CONTEXT.md | WIRED | readFileSync calls with try/catch; reads all 3 file types |
| `packages/api/src/commands/crystallize.ts` | `packages/api/src/review/seed-writer.ts` | readSeedDraft | WIRED | Line 4: `import { readSeedDraft }` |
| `packages/api/src/commands/status.ts` | `packages/shared/src/db/schema/bead.ts` | drizzle query on beads table | WIRED | `import { beads } from '@cauldron/shared'`; drizzle `.from(beads).where(eq(beads.seedId, seedId))` |
| `packages/api/src/commands/kill.ts` | `packages/shared/src/db/event-store.ts` | appendEvent for bead_failed | WIRED | `import { appendEvent } from '@cauldron/shared'`; called with `type: 'bead_failed'` |
| `packages/api/src/commands/execute.ts` | `packages/engine/src/decomposition/events.ts` | inngest.send for bead.dispatch_requested | WIRED | `inngest.send({ name: 'bead.dispatch_requested', ... })` |
| `packages/api/src/commands/execute.ts` | hono | Hono HTTP server for Inngest handler | WIRED | `new Hono()`, `serve({ fetch: app.fetch, port: 3001 })` |
| `packages/api/src/self-build.ts` | git rev-parse HEAD | child_process exec for snapshot | WIRED | `execSync('git rev-parse HEAD', { cwd: projectRoot })` |
| `.claude/skills/cauldron-*.md` | `packages/api/src/cli.ts` | pnpm exec tsx invocation | WIRED | All 8 skills reference `pnpm exec tsx packages/api/src/cli.ts` |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase produces CLI tools and skill invocation files, not dashboard/page components rendering dynamic data.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| API unit tests (42 tests) | `pnpm --filter @cauldron/api test` | 12 test files, 42 tests pass | PASS |
| API typecheck | `pnpm --filter @cauldron/api typecheck` | No type errors (exit 0) | PASS |
| Engine tests (no regressions) | `pnpm --filter @cauldron/engine test` | 25 test files, 268 tests pass | PASS |
| Full monorepo build | `pnpm build` | 4 tasks successful, all cached | PASS |
| Full typecheck | `pnpm typecheck` | 6 tasks successful, no errors | PASS |
| All 8 skill files exist | `ls .claude/skills/cauldron-*.md` | 8 files confirmed | PASS |
| selfBuild flag in config | `grep selfBuild cauldron.config.ts` | `selfBuild: true` present | PASS |
| CLI health endpoint corrected | inspect health.ts | `/v1/events` (not `/v0/envs`) | PASS |
| D-09 end-to-end pipeline run | run pipeline on separate CLI rename repo | Not performed — checkpoint scoped to build/test only | FAIL |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | Plan 05 | Separate project-local Claude Code skills per pipeline stage | SATISFIED | 8 skill files in .claude/skills/ |
| D-02 | Plan 01, 05 | Skills invoke CLI entrypoint via tsx; in-process execution | SATISFIED | cli.ts with parseArgs routing; skills reference pnpm exec tsx |
| D-03 | Plan 04 | Fire-and-poll execution model; execute kicks off and returns | SATISFIED | execute.ts dispatches and stays alive for polling |
| D-04 | Plan 02 | Pipeline state stored in PostgreSQL; status queries DB | SATISFIED | status.ts queries beads table via Drizzle |
| D-05 | Plan 01, 05 | Skills include health checks before running | SATISFIED | healthCheck() in all skills + cli.ts pre-flight |
| D-06 | Plan 03, 04 | Full pipeline path: interview → crystallize → holdouts → seal → decompose → execute | SATISFIED | All pipeline commands implemented |
| D-07 | Plan 03 | Bootstrap from GSD context — interview reads .planning/ as prior requirements context | SATISFIED | readPlanningArtifacts in context-bridge.ts; interview.ts injects prior context |
| D-08 | Plan 04 | Include holdout test generation + encryption in dogfood path | SATISFIED | seal.ts with generateHoldoutScenarios + sealVault |
| D-09 | Plan 05 | Dry run on trivial project first — validate pipeline on v1 test case | PARTIAL | Build/test/typecheck verified; actual pipeline run on separate external repo not performed |
| D-10 | Plan 03 | Seed acceptance criteria map back to GSD requirement IDs | SATISFIED | extractRequirementIds in context-bridge.ts; used in interview prior-context injection |
| D-11 | Plan 01, 05 | CLI entrypoint first, skills wrap it; specified build order followed | SATISFIED | cli.ts built in Plan 01; all skills wrap it |
| D-12 | Plan 04 | Engine snapshot at run start when selfBuild is true | SATISFIED | captureEngineSnapshot in self-build.ts; execute.ts captures when selfBuild: true |
| D-13 | Plan 04 | Migrations allowed with review gate — flagged for human review | SATISFIED | hasMigrationFiles and getBeadDiff in self-build.ts for migration detection |
| D-14 | Plan 04 | Auto-revert on post-merge test failure | SATISFIED | Leverages Phase 6 merge queue; detectEngineChange warns on engine change |
| D-15 | Plan 01 | Explicit selfBuild: true config flag activates extra guardrails | SATISFIED | selfBuild?: boolean in GatewayConfig; cauldron.config.ts sets selfBuild: true |
| D-16 | Plan 02, 03 | File-based seed review — interview outputs seed YAML draft | SATISFIED | writeSeedDraft/readSeedDraft in seed-writer.ts |
| D-17 | Plan 02, 04 | File-based holdout review — scenarios written to review file | SATISFIED | writeHoldoutDraft/readHoldoutDraft in holdout-writer.ts |
| D-18 | Plan 02 | Status table + optional log tail | SATISFIED | statusCommand with console.table and --logs flag |
| D-19 | Plan 02, 04 | Merge conflict escalation via status flag + file diff | SATISFIED | NEEDS REVIEW status in statusCommand; conflict file in resolve.ts |
| D-20 | Plan 02 | Kill only for bead intervention | SATISFIED | killCommand marks bead failed and appends event |
| D-21 | Plan 04 | Resume from DB state — --resume skips completed beads, re-dispatches failed/pending | SATISFIED | execute.ts --resume flag resets failed→pending before dispatching |

**Orphaned requirements:** None. All D-01 through D-21 are claimed in plan frontmatter requirements fields.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `packages/api/src/commands/interview.ts` | `process.exit(0)` at end | Info | Expected in CLI commands — correct behavior |
| Multiple command files | Self-bootstrapping pattern (`bootstrap()` called internally in each command) | Info | Intentional design decision per Plan 03; keeps cli.ts routing minimal |
| `packages/api/src/cli.ts` lines 94-100 | kill command reads `CAULDRON_PROJECT_ID` env var instead of --project-id flag | Warning | Inconsistent with other commands that use --project-id flag via parseArgs; kill.ts uses KillDeps interface requiring projectId from env. Does not block goal but creates UX inconsistency. |

No blocker anti-patterns found. No TODO/FIXME/placeholder comments in implementation files. No empty `return {}` or `return []` stubs — all commands are substantive.

---

### Human Verification Required

#### 1. D-09: End-to-End Pipeline Dry Run on External Project

**Test:** Create a minimal separate git repo (e.g., `~/tmp/cli-rename-tool`) with a `package.json`, some source files, and a `cauldron.config.ts` with `selfBuild: false`. Run the full Cauldron pipeline against it using the skills: `/cauldron:interview`, `/cauldron:crystallize`, `/cauldron:seal --generate`, `/cauldron:decompose`, `/cauldron:execute`. Observe whether beads are dispatched and at least one completes.
**Expected:** The pipeline completes at least through decompose (bead DAG created) without errors. `/cauldron:status` shows beads in the table.
**Why human:** Requires running Docker Compose services, an interactive interview session against a real external project, and observing live Inngest event processing. Cannot verify programmatically.

---

### Gaps Summary

One gap found blocking full D-09 goal achievement:

**D-09 dry-run on external project was not completed.** The CONTEXT.md specification states: "Dry run on trivial project first — validate the pipeline on the v1 test case (CLI bulk file renaming tool) in a separate repo before attempting self-build on Phase 7." Plan 05 scoped the Task 2 checkpoint to verifying build/test/typecheck — all of which pass — but did not include creating a separate repo or running the actual interview → execute pipeline against it.

The gap is scoped: all CLI infrastructure is verified working. The missing piece is the live end-to-end validation on an external project. This is a pipeline validation checkpoint, not a code correctness issue.

**Possible resolution paths:**
1. Run the dry-run now, document results, and mark D-09 satisfied if successful (recommended — 30 min effort)
2. Explicitly defer to a pre-Phase-7 check with documented risk acceptance

---

_Verified: 2026-03-26T18:55:51Z_
_Verifier: Claude (gsd-verifier)_
