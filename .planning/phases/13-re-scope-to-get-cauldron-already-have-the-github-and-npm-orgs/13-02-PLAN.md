---
phase: 13-re-scope-to-get-cauldron-already-have-the-github-and-npm-orgs
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - packages/engine/src/**/*.ts
  - packages/cli/src/**/*.ts
  - packages/web/src/**/*.ts
  - packages/web/src/**/*.tsx
  - packages/web/next.config.ts
  - packages/engine/vitest.integration.config.ts
  - packages/cli/vitest.integration.config.ts
  - scripts/run-interview-automated.ts
  - scripts/inject-cli-renamer-seed.ts
  - cauldron.config.ts
  - .claude/skills/*.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements:
  - SC-4
  - SC-5
  - SC-6

must_haves:
  truths:
    - "Zero occurrences of @cauldron/ in any source file, import, or workspace dep"
    - "All tests pass after rename"
    - "Typecheck passes after rename"
    - "Build succeeds after rename"
    - "CLAUDE.md references new @get-cauldron/* scope"
    - "Claude Code skills reference packages/cli (not packages/api)"
    - "Active planning docs (ROADMAP.md, STATE.md, research/ARCHITECTURE.md) reference new scope"
  artifacts:
    - path: "packages/cli/src/trpc-client.ts"
      provides: "tRPC client with AppRouter from @get-cauldron/shared/trpc-types"
      contains: "@get-cauldron/shared/trpc-types"
    - path: "packages/web/next.config.ts"
      provides: "Next.js config with transpile packages"
      contains: "@get-cauldron/shared"
    - path: "cauldron.config.ts"
      provides: "Project config importing from new scope"
      contains: "@get-cauldron/engine/gateway"
  key_links:
    - from: "packages/cli/src/trpc-client.ts"
      to: "packages/shared/src/trpc-types.ts"
      via: "@get-cauldron/shared/trpc-types import"
      pattern: "from '@get-cauldron/shared/trpc-types'"
    - from: "packages/engine/src/**/*.ts"
      to: "packages/shared/src/index.ts"
      via: "@get-cauldron/shared import"
      pattern: "from '@get-cauldron/shared'"
    - from: "packages/web/src/**/*.ts"
      to: "packages/engine/src/index.ts"
      via: "@get-cauldron/engine import"
      pattern: "from '@get-cauldron/engine'"
---

<objective>
Update all source file imports from @cauldron/* to @get-cauldron/*, update config files, scripts, skills, and documentation. Run full regression gate.

Purpose: Complete the big-bang rename so the entire codebase compiles and all tests pass under the new @get-cauldron/* scope.
Output: Zero @cauldron/ references in source code, all tests green, build succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-re-scope-to-get-cauldron-already-have-the-github-and-npm-orgs/13-CONTEXT.md
@.planning/phases/13-re-scope-to-get-cauldron-already-have-the-github-and-npm-orgs/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all source file imports and config references from @cauldron/* to @get-cauldron/*</name>
  <files>
    packages/engine/src/gateway/gateway.ts
    packages/engine/src/gateway/budget.ts
    packages/engine/src/gateway/__tests__/gateway.test.ts
    packages/engine/src/gateway/__tests__/budget.test.ts
    packages/engine/src/decomposition/events.ts
    packages/engine/src/decomposition/decomposer.ts
    packages/engine/src/decomposition/pipeline.ts
    packages/engine/src/decomposition/scheduler.ts
    packages/engine/src/decomposition/__tests__/decomposer.test.ts
    packages/engine/src/decomposition/__tests__/scheduler.test.ts
    packages/engine/src/decomposition/__tests__/events.test.ts
    packages/engine/src/decomposition/__tests__/concurrent-claim.integration.test.ts
    packages/engine/src/__tests__/setup.ts
    packages/engine/src/interview/fsm.ts
    packages/engine/src/interview/crystallizer.ts
    packages/engine/src/interview/__tests__/fsm.test.ts
    packages/engine/src/interview/__tests__/seed-injection.test.ts
    packages/engine/src/interview/__tests__/fsm-sendAnswer.integration.test.ts
    packages/engine/src/execution/merge-queue.ts
    packages/engine/src/execution/context-assembler.ts
    packages/engine/src/execution/types.ts
    packages/engine/src/execution/__tests__/merge-queue.test.ts
    packages/engine/src/execution/__tests__/context-assembler.test.ts
    packages/engine/src/evolution/events.ts
    packages/engine/src/evolution/convergence.ts
    packages/engine/src/evolution/budget.ts
    packages/engine/src/evolution/mutator.ts
    packages/engine/src/evolution/evaluator.ts
    packages/engine/src/evolution/lateral-thinking.ts
    packages/engine/src/evolution/__tests__/fsm.test.ts
    packages/engine/src/evolution/__tests__/mutator.test.ts
    packages/engine/src/evolution/__tests__/convergence.test.ts
    packages/engine/src/evolution/__tests__/budget.test.ts
    packages/engine/src/evolution/__tests__/evaluator.test.ts
    packages/engine/src/evolution/__tests__/lateral-thinking.test.ts
    packages/engine/src/holdout/events.ts
    packages/engine/src/holdout/vault.ts
    packages/engine/src/holdout/generator.ts
    packages/engine/src/holdout/__tests__/generator.test.ts
    packages/engine/src/holdout/__tests__/vault.test.ts
    packages/engine/src/holdout/__tests__/events.test.ts
    packages/engine/src/holdout/__tests__/evaluator.test.ts
    packages/engine/src/index.ts
    packages/engine/vitest.integration.config.ts
    packages/cli/src/bootstrap.ts
    packages/cli/src/index.ts
    packages/cli/src/trpc-client.ts
    packages/cli/src/health.ts
    packages/cli/src/config-io.ts
    packages/cli/src/inngest-serve.ts
    packages/cli/src/review/holdout-writer.ts
    packages/cli/src/review/seed-writer.ts
    packages/cli/src/__tests__/bootstrap.test.ts
    packages/cli/src/__tests__/health.test.ts
    packages/cli/src/__tests__/inngest-serve.test.ts
    packages/cli/src/__tests__/review-writer.test.ts
    packages/cli/src/__tests__/seal-command.test.ts
    packages/cli/src/__tests__/interview-command.test.ts
    packages/cli/src/__tests__/crystallize.test.ts
    packages/cli/src/__tests__/resolve-command.test.ts
    packages/cli/src/__tests__/status.test.ts
    packages/cli/vitest.integration.config.ts
    packages/web/next.config.ts
    packages/web/src/inngest/pipeline-trigger.ts
    packages/web/src/app/api/events/__tests__/route.test.ts
    packages/web/src/app/api/events/[projectId]/route.ts
    packages/web/src/app/api/webhook/git/route.ts
    packages/web/src/app/api/webhook/git/route.test.ts
    packages/web/src/trpc/init.ts
    packages/web/src/trpc/engine-deps.ts
    packages/web/src/trpc/routers/execution.ts
    packages/web/src/trpc/routers/interview.ts
    packages/web/src/trpc/routers/evolution.ts
    packages/web/src/trpc/routers/costs.ts
    packages/web/src/trpc/routers/projects.ts
    packages/web/src/trpc/routers/__tests__/seal-decompose-engine.test.ts
    packages/web/src/trpc/routers/__tests__/interview-engine.test.ts
    packages/web/src/app/projects/[id]/interview/page.tsx
    scripts/run-interview-automated.ts
    scripts/inject-cli-renamer-seed.ts
    cauldron.config.ts
  </files>
  <read_first>
    packages/cli/src/trpc-client.ts
    packages/cli/src/bootstrap.ts
    packages/cli/src/config-io.ts
    packages/cli/src/index.ts
    packages/web/next.config.ts
    packages/engine/src/index.ts
    cauldron.config.ts
  </read_first>
  <action>
    Perform a global find-and-replace across ALL source files (.ts, .tsx) in `packages/` and root `scripts/` and `cauldron.config.ts`. The replacements are:

    **Import/require replacements (in this order to avoid double-replacing):**

    1. `@cauldron/trpc-types` -> `@get-cauldron/shared/trpc-types`
       - Only occurrence: `packages/cli/src/trpc-client.ts` line 2
       - Change: `import type { AppRouter } from '@cauldron/trpc-types';` -> `import type { AppRouter } from '@get-cauldron/shared/trpc-types';`

    2. `@cauldron/engine` -> `@get-cauldron/engine`
       - Applies to all import statements, vi.mock() calls, and comments referencing the package
       - ~25 files across engine tests, cli, and web packages

    3. `@cauldron/shared` -> `@get-cauldron/shared`
       - Applies to all import statements, vi.mock() calls, dynamic import() calls, and comments
       - ~65 files across all packages

    4. `@cauldron/web` -> `@get-cauldron/web` (if any source imports exist)

    5. `@cauldron/cli` -> `@get-cauldron/cli` (if any source imports exist)

    **Specific file-level changes:**

    - `packages/engine/src/index.ts`: Update comment from `// @cauldron/engine entry point` to `// @get-cauldron/engine entry point`
    - `packages/cli/src/index.ts`: Update comment from `// @cauldron/api entry point` to `// @get-cauldron/cli entry point`
    - `packages/cli/src/config-io.ts`: Update the template string that generates cauldron.config.ts content:
      Change `import { defineConfig } from '@cauldron/engine/gateway';` to `import { defineConfig } from '@get-cauldron/engine/gateway';`
    - `packages/web/next.config.ts`: Change `transpilePackages: ['@cauldron/shared', '@cauldron/engine']` to `transpilePackages: ['@get-cauldron/shared', '@get-cauldron/engine']`
    - `packages/engine/vitest.integration.config.ts`: Update comment referencing `@cauldron/shared`
    - `packages/cli/vitest.integration.config.ts`: Update comment referencing `@cauldron/shared`
    - `cauldron.config.ts`: Change `import { defineConfig } from '@cauldron/engine/gateway';` to `import { defineConfig } from '@get-cauldron/engine/gateway';`
    - `scripts/run-interview-automated.ts`: Change `from '@cauldron/shared'` to `from '@get-cauldron/shared'` and `from '@cauldron/engine'` to `from '@get-cauldron/engine'`
    - `scripts/inject-cli-renamer-seed.ts`: Change `from '@cauldron/shared'` to `from '@get-cauldron/shared'`

    **Efficient approach:** Use `sed` or a script to do the bulk replacement:
    ```bash
    # From repo root, replace in all .ts and .tsx files under packages/ and scripts/
    find packages/ scripts/ -name '*.ts' -o -name '*.tsx' | xargs sed -i '' "s/@cauldron\/trpc-types/@get-cauldron\/shared\/trpc-types/g"
    find packages/ scripts/ -name '*.ts' -o -name '*.tsx' | xargs sed -i '' "s/@cauldron\/engine/@get-cauldron\/engine/g"
    find packages/ scripts/ -name '*.ts' -o -name '*.tsx' | xargs sed -i '' "s/@cauldron\/shared/@get-cauldron\/shared/g"
    find packages/ scripts/ -name '*.ts' -o -name '*.tsx' | xargs sed -i '' "s/@cauldron\/web/@get-cauldron\/web/g"
    find packages/ scripts/ -name '*.ts' -o -name '*.tsx' | xargs sed -i '' "s/@cauldron\/cli/@get-cauldron\/cli/g"
    # Also the root config file
    sed -i '' "s/@cauldron\/engine/@get-cauldron\/engine/g" cauldron.config.ts
    ```

    **CRITICAL ORDER:** Replace `@cauldron/trpc-types` FIRST (before replacing `@cauldron/shared` which would partially match). Then engine, then shared, then web, then cli.

    After replacement, manually verify `packages/cli/src/trpc-client.ts` contains `from '@get-cauldron/shared/trpc-types'` (not double-replaced).
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && ! grep -r "@cauldron/" packages/ scripts/ cauldron.config.ts --include="*.ts" --include="*.tsx" --exclude-dir=dist --exclude-dir=node_modules --exclude-dir=.turbo | grep -v ".claude/worktrees" | grep -v pnpm-lock && echo "ZERO_OCCURRENCES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@cauldron/" packages/ --include="*.ts" --include="*.tsx" --exclude-dir=dist` returns zero results (excluding node_modules and .claude/worktrees)
    - `grep -r "@cauldron/" scripts/ --include="*.ts"` returns zero results
    - `grep "@cauldron/" cauldron.config.ts` returns zero results
    - `packages/cli/src/trpc-client.ts` contains `from '@get-cauldron/shared/trpc-types'`
    - `packages/web/next.config.ts` contains `'@get-cauldron/shared', '@get-cauldron/engine'`
    - `packages/cli/src/config-io.ts` contains `from '@get-cauldron/engine/gateway'` with @get-cauldron prefix
    - `packages/engine/src/index.ts` contains `@get-cauldron/engine`
    - `packages/cli/src/index.ts` contains `@get-cauldron/cli`
  </acceptance_criteria>
  <done>All source files, scripts, and config files reference @get-cauldron/* instead of @cauldron/*. Zero occurrences of the old scope remain in source (dist/ build artifacts excluded until rebuilt).</done>
</task>

<task type="auto">
  <name>Task 2: Update Claude Code skills, CLAUDE.md, active planning docs, and run full regression gate</name>
  <files>
    .claude/skills/cauldron-crystallize.md
    .claude/skills/cauldron-decompose.md
    .claude/skills/cauldron-execute.md
    .claude/skills/cauldron-interview.md
    .claude/skills/cauldron-kill.md
    .claude/skills/cauldron-resolve.md
    .claude/skills/cauldron-seal.md
    .claude/skills/cauldron-status.md
    CLAUDE.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/research/ARCHITECTURE.md
  </files>
  <read_first>
    .claude/skills/cauldron-crystallize.md
    .claude/skills/cauldron-interview.md
    .claude/skills/cauldron-status.md
    CLAUDE.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    **Step 1: Update all 8 Claude Code skill files**

    In every `.claude/skills/cauldron-*.md` file, replace all occurrences of `packages/api/src/cli.ts` with `packages/cli/src/cli.ts`.

    The affected files and their commands:
    - `cauldron-crystallize.md`: `pnpm exec tsx packages/api/src/cli.ts health` -> `pnpm exec tsx packages/cli/src/cli.ts health`, and `pnpm exec tsx packages/api/src/cli.ts crystallize` -> `pnpm exec tsx packages/cli/src/cli.ts crystallize`
    - `cauldron-decompose.md`: same pattern for `health` and `decompose` commands
    - `cauldron-execute.md`: same pattern for `health` and `execute` commands
    - `cauldron-interview.md`: same pattern for `health` and `interview` commands
    - `cauldron-kill.md`: same pattern for `health` and `kill` commands
    - `cauldron-resolve.md`: same pattern for `health` and `resolve` commands
    - `cauldron-seal.md`: same pattern for `health` and `seal` commands (3 occurrences)
    - `cauldron-status.md`: same pattern for `health` and `status` commands

    **Step 2: Update CLAUDE.md**

    Search CLAUDE.md for any references to:
    - `@cauldron/` -> replace with `@get-cauldron/`
    - `packages/api` -> replace with `packages/cli`

    Note: CLAUDE.md is large and contains the full technology stack documentation. The references are primarily in:
    - Installation/monorepo scaffold sections mentioning package names
    - Stack patterns mentioning package imports
    - Any architecture references to `@cauldron/*`

    **Step 3: Update active planning docs (per D-06, SC-6)**

    These are active navigation documents consulted in future planning -- NOT historical phase artifacts.

    - `.planning/ROADMAP.md`: Replace `@cauldron/` with `@get-cauldron/` and `packages/api` with `packages/cli` in all active content
    - `.planning/STATE.md`: Replace `@cauldron/` with `@get-cauldron/` and `packages/api` with `packages/cli` in all active content
    - `.planning/research/ARCHITECTURE.md`: If this file exists, replace `@cauldron/` with `@get-cauldron/` and `packages/api` with `packages/cli`

    Do NOT update historical phase artifacts (files matching `*-PLAN.md`, `*-SUMMARY.md`, `*-VERIFICATION.md`, `*-CONTEXT.md`, `*-RESEARCH.md`, `*-DISCUSSION-LOG.md`, `*-VALIDATION.md` under `.planning/phases/`). Those are historical records of what was true at the time and should be preserved as-is.

    **Step 4: Run full regression gate (per D-05 atomic rename, SC-5)**

    Run in sequence:
    1. `pnpm install` (ensure lockfile is correct after all changes)
    2. `pnpm -r typecheck` (TypeScript compilation across all packages)
    3. `pnpm -r test` (all unit tests across all packages)
    4. `pnpm -r build` (build all packages)

    If any step fails, diagnose and fix. Common issues:
    - Missed import replacement (grep for `@cauldron/` to find stragglers)
    - tsconfig path issues after directory rename
    - Lockfile out of sync (re-run pnpm install)

    **Step 5: Verify zero remaining @cauldron/ references in non-historical files**

    Run: `grep -r "@cauldron/" . --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" --include="*.yaml" --exclude-dir=dist --exclude-dir=node_modules --exclude-dir=.turbo | grep -v ".claude/worktrees" | grep -v pnpm-lock | grep -v ".planning/phases/"`

    This excludes:
    - `dist/` directories (stale build artifacts, cleaned by build step)
    - `node_modules/` (external dependencies)
    - `.turbo/` (build cache)
    - `.claude/worktrees/` (parallel agent worktrees)
    - `pnpm-lock.yaml` (regenerated by pnpm install)
    - `.planning/phases/` (historical phase artifacts only)

    Active planning docs (`.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/research/`) are NOT excluded and MUST show zero hits. Any remaining hits need to be fixed.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm install && pnpm -r typecheck && pnpm -r test && pnpm -r build && echo "REGRESSION_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 `.claude/skills/cauldron-*.md` files contain `packages/cli/src/cli.ts` (not `packages/api/src/cli.ts`)
    - `grep "packages/api" .claude/skills/*.md` returns zero results
    - `CLAUDE.md` contains no `@cauldron/` references (only `@get-cauldron/`)
    - `.planning/ROADMAP.md` contains no `@cauldron/` references
    - `.planning/STATE.md` contains no `@cauldron/` references
    - `.planning/research/ARCHITECTURE.md` contains no `@cauldron/` references (if file exists)
    - `pnpm -r typecheck` exits 0
    - `pnpm -r test` exits 0
    - `pnpm -r build` exits 0
    - `grep -r "@cauldron/" . --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" --exclude-dir=dist --exclude-dir=node_modules --exclude-dir=.turbo | grep -v ".claude/worktrees" | grep -v pnpm-lock | grep -v ".planning/phases/"` returns zero results
  </acceptance_criteria>
  <done>Claude Code skills updated with new paths, CLAUDE.md updated with new scope, active planning docs (ROADMAP.md, STATE.md, research/ARCHITECTURE.md) updated with new scope, full regression gate passes (typecheck + tests + build). Zero @cauldron/ references remain in active project files.</done>
</task>

</tasks>

<verification>
- `grep -rn "@cauldron/" packages/ scripts/ cauldron.config.ts .claude/skills/ CLAUDE.md .planning/ROADMAP.md .planning/STATE.md .planning/research/ARCHITECTURE.md --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" --exclude-dir=dist --exclude-dir=node_modules` returns zero results
- `pnpm -r typecheck` passes
- `pnpm -r test` passes
- `pnpm -r build` passes
- All skill files reference `packages/cli/src/cli.ts`
</verification>

<success_criteria>
- Zero occurrences of @cauldron/ in source files, imports, workspace deps, config files, skills, CLAUDE.md, or active planning docs
- All tests pass across all packages
- TypeScript compilation succeeds across all packages
- Build succeeds across all packages
- Claude Code skills reference correct paths (packages/cli not packages/api)
- Active planning docs (ROADMAP.md, STATE.md, research/ARCHITECTURE.md) reference @get-cauldron/* scope
</success_criteria>

<output>
After completion, create `.planning/phases/13-re-scope-to-get-cauldron-already-have-the-github-and-npm-orgs/13-02-SUMMARY.md`
</output>
