---
phase: 16-bridge-evolution-loop-fix-bead-dispatch
plan: "03"
type: execute
wave: 1
depends_on: ["16-02"]
files_modified:
  - packages/web/src/trpc/routers/__tests__/execution.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [WEB-04, EXEC-03]

must_haves:
  truths:
    - "triggerExecution dispatches individual bead.dispatch_requested events with beadId for each ready bead — tested and verified by execution.test.ts"
    - "WEB-04 requirement tracker shows Complete"
  artifacts:
    - path: "packages/web/src/trpc/routers/__tests__/execution.test.ts"
      provides: "Unit tests for triggerExecution per-bead dispatch behavior"
      contains: "findReadyBeads"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement tracker"
      contains: "WEB-04 | Phase 16 | Complete"
  key_links:
    - from: "packages/web/src/trpc/routers/__tests__/execution.test.ts"
      to: "packages/web/src/trpc/routers/execution.ts"
      via: "import and mock verification"
      pattern: "findReadyBeads|engineInngest\\.send"
---

<objective>
Close two verification gaps from Phase 16: (1) create the missing execution.test.ts with tests verifying triggerExecution per-bead dispatch behavior, and (2) update WEB-04 requirement status from Pending to Complete.

Purpose: The runtime code is correct but test coverage for the per-bead dispatch path was never created, and the requirement tracker was not updated.
Output: execution.test.ts with passing tests, updated REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bridge-evolution-loop-fix-bead-dispatch/16-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/web/src/trpc/routers/execution.ts (triggerExecution mutation, lines 98-128):
```typescript
triggerExecution: publicProcedure
  .input(z.object({
    projectId: z.string().uuid(),
    seedId: z.string().uuid(),
  }))
  .mutation(async ({ ctx, input }) => {
    await appendEvent(ctx.db, { ... type: 'execution_started' ... });
    const readyBeads = await findReadyBeads(ctx.db, input.seedId);
    for (const bead of readyBeads) {
      await engineInngest.send({
        name: 'bead.dispatch_requested',
        data: { beadId: bead.id, seedId: input.seedId, projectId: input.projectId, moleculeId: bead.moleculeId },
      });
    }
    return { success: true, message: `Execution triggered: ${readyBeads.length} beads dispatched` };
  })
```

From packages/web/src/trpc/routers/execution.ts imports (line 6):
```typescript
import { runDecomposition, inngest as engineInngest, findReadyBeads } from '@get-cauldron/engine';
```

Existing test pattern from packages/web/src/trpc/routers/__tests__/interview-engine.test.ts:
- Uses vi.mock('@get-cauldron/engine', ...) to mock engine exports
- Uses vi.mock('@get-cauldron/shared', ...) to mock DB/schema
- Uses vi.mock('drizzle-orm', ...) to mock query helpers
- Builds fake tRPC context with fluent db.select().from().where() chain
- Tests call router procedures via createCaller pattern or direct procedure invocation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create execution.test.ts with per-bead dispatch tests</name>
  <files>packages/web/src/trpc/routers/__tests__/execution.test.ts</files>
  <read_first>
    - packages/web/src/trpc/routers/execution.ts
    - packages/web/src/trpc/routers/__tests__/interview-engine.test.ts
    - packages/web/src/trpc/routers/__tests__/seal-decompose-engine.test.ts
  </read_first>
  <action>
Create `packages/web/src/trpc/routers/__tests__/execution.test.ts` following the exact mock patterns from the existing test files in the same directory.

Mock setup (match interview-engine.test.ts pattern):
- `const mockFindReadyBeads = vi.fn()` and `const mockInngestSend = vi.fn()`
- `vi.mock('@get-cauldron/engine', () => ({ findReadyBeads: (...args) => mockFindReadyBeads(...args), inngest: { send: (...args) => mockInngestSend(...args) }, runDecomposition: vi.fn() }))`
- `vi.mock('@get-cauldron/shared', () => ({ db: {}, appendEvent: vi.fn(), beads: {}, beadEdges: {}, events: {}, seeds: {} }))`
- `vi.mock('drizzle-orm', () => ({ eq: vi.fn(), desc: vi.fn(), inArray: vi.fn() }))`

Build a `makeCtx()` helper that returns a fake tRPC context with `db` (fluent select chain) and `getEngineDeps` (returns mock gateway/config/logger). Follow the same fluent builder pattern as interview-engine.test.ts for `db.select().from().where()`.

Write three test cases in a `describe('triggerExecution')` block:

1. **"calls findReadyBeads with seedId and dispatches one event per bead"** — `mockFindReadyBeads` returns 2 beads with `{ id: 'bead-1', moleculeId: 'mol-1' }` and `{ id: 'bead-2', moleculeId: 'mol-2' }`. Assert `mockFindReadyBeads` called with `(ctx.db, seedId)`. Assert `mockInngestSend` called twice. Assert first call has `data.beadId === 'bead-1'` and second has `data.beadId === 'bead-2'`. Assert both calls have `name: 'bead.dispatch_requested'` and include `seedId`, `projectId`, `moleculeId`.

2. **"dispatches nothing when findReadyBeads returns empty array"** — `mockFindReadyBeads` returns `[]`. Assert `mockInngestSend` not called. Assert result message contains "0 beads dispatched".

3. **"includes moleculeId in dispatch payload"** — `mockFindReadyBeads` returns one bead with `moleculeId: null`. Assert the send call includes `moleculeId: null` (not undefined, not omitted).

Import the `executionRouter` and call `triggerExecution` via the router's procedure. The exact invocation pattern should match how the existing tests in this directory call their routers — read both test files to confirm.

IMPORTANT: Reset all mocks in `beforeEach` with `vi.clearAllMocks()`.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm -F @get-cauldron/web test -- src/trpc/routers/__tests__/execution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File packages/web/src/trpc/routers/__tests__/execution.test.ts exists
    - Contains "findReadyBeads" (grep-verifiable)
    - Contains "bead.dispatch_requested" (grep-verifiable)
    - Contains "0 beads dispatched" or "dispatches nothing" (zero-bead test case)
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>execution.test.ts exists with 3 passing tests covering: per-bead dispatch with beadId, zero-bead case, moleculeId inclusion</done>
</task>

<task type="auto">
  <name>Task 2: Update WEB-04 requirement status to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, find the line:
```
| WEB-04 | Phase 16 | Pending |
```
Change it to:
```
| WEB-04 | Phase 16 | Complete |
```

This is justified because: Phase 8 built the SSE streaming infrastructure (route handler, useSSE hook, event polling). Phase 16 fixed the auth gap (query-param token fallback) that prevented web SSE connections when CAULDRON_API_KEY was set. Together, Phases 8+16 satisfy "Real-time streaming of agent logs and code diffs via SSE" (WEB-04).
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && grep "WEB-04" .planning/REQUIREMENTS.md | grep "Complete"</automated>
  </verify>
  <acceptance_criteria>
    - grep "WEB-04.*Complete" .planning/REQUIREMENTS.md returns a match
    - No other requirement statuses were changed
  </acceptance_criteria>
  <done>WEB-04 shows Complete in the requirement tracker</done>
</task>

</tasks>

<verification>
1. `pnpm -F @get-cauldron/web test -- src/trpc/routers/__tests__/execution.test.ts` exits 0
2. `pnpm -F @get-cauldron/web test` passes (all existing + new tests)
3. `grep "WEB-04.*Complete" .planning/REQUIREMENTS.md` returns a match
</verification>

<success_criteria>
- execution.test.ts exists and all 3 tests pass
- WEB-04 requirement tracker shows Complete
- No regressions in existing web test suite
</success_criteria>

<output>
After completion, create `.planning/phases/16-bridge-evolution-loop-fix-bead-dispatch/16-03-SUMMARY.md`
</output>
