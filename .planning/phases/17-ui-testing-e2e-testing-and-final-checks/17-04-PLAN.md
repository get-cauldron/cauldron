---
phase: 17-ui-testing-e2e-testing-and-final-checks
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/web/e2e/execution.spec.ts
  - packages/web/e2e/evolution.spec.ts
  - packages/web/e2e/costs.spec.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-13
  - D-15

must_haves:
  truths:
    - "E2E tests cover DAG execution page with bead detail interaction"
    - "E2E tests cover evolution page with seed lineage and convergence display"
    - "E2E tests cover costs page with token usage data"
    - "axe-core runs on all three pages"
    - "Visual snapshots captured for execution, evolution, and costs pages"
    - "Execution page SSE test verifies bead status update via DB insert"
  artifacts:
    - path: "packages/web/e2e/execution.spec.ts"
      provides: "DAG execution E2E tests"
      min_lines: 50
    - path: "packages/web/e2e/evolution.spec.ts"
      provides: "Evolution page E2E tests"
      min_lines: 40
    - path: "packages/web/e2e/costs.spec.ts"
      provides: "Costs page E2E tests"
      min_lines: 30
  key_links:
    - from: "packages/web/e2e/execution.spec.ts"
      to: "packages/web/e2e/helpers/db.ts"
      via: "createTestBead, createTestEvent"
      pattern: "createTestBead"
    - from: "packages/web/e2e/execution.spec.ts"
      to: "events table"
      via: "SSE polling from /api/events/[projectId]"
      pattern: "bead_dispatched|bead_completed"
---

<objective>
Create Playwright E2E tests for the execution (DAG), evolution, and costs pages -- the pipeline monitoring and analysis surfaces.

Purpose: Completes D-01 (full surface E2E) for the remaining three dashboard pages. Execution page tests verify DAG rendering with pre-seeded bead data and SSE live updates. Evolution page tests verify seed lineage and convergence panels. Costs page tests verify token usage display. All specs run Chromium only per D-04.
Output: 3 E2E spec files completing full dashboard coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-testing-e2e-testing-and-final-checks/17-01-SUMMARY.md
@packages/web/playwright.config.ts
@packages/web/e2e/helpers/db.ts
@packages/web/e2e/helpers/accessibility.ts
@packages/web/e2e/helpers/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E execution spec with DAG rendering and SSE verification</name>
  <files>
    packages/web/e2e/execution.spec.ts
  </files>
  <read_first>
    packages/web/e2e/helpers/db.ts
    packages/web/e2e/helpers/accessibility.ts
    packages/web/e2e/helpers/routes.ts
    packages/web/src/app/projects/[id]/execution/page.tsx
    packages/web/src/components/dag/DAGCanvas.tsx
    packages/web/src/components/dag/BeadNode.tsx
    packages/web/src/components/bead/BeadDetailSheet.tsx
    packages/shared/src/db/schema/index.ts
  </read_first>
  <action>
    **Create `packages/web/e2e/execution.spec.ts` (per D-01, D-04, D-13, D-15):**

    **Important scoping decision (from research pitfall 6):** Do NOT attempt live Inngest dispatch in E2E. Scope to pre-seeded bead data — test DAG rendering, bead interactions, and SSE status updates via direct DB inserts.

    **Setup:** Create DB connection, run migrations. Seed a project + seed + multiple beads (3-5 beads with various statuses: pending, active, completed, failed) + bead_edges connecting them. This creates a testable DAG without needing Inngest.

    **Tests:**
    1. `test('execution page renders DAG with seeded beads')` — Seed project with beads and edges. Navigate to `ROUTES.execution(projectId)`. Wait for DAG canvas to be visible. Assert bead count matches seeded data (check for data-testid or role attributes on bead nodes). `assertNoA11yViolations(page)`. `expect(page).toHaveScreenshot('execution-dag.png', { threshold: 0.1, animations: 'disabled' })`.

    2. `test('clicking a bead opens detail sheet')` — Seed project with beads. Navigate to execution page. Click on a bead node element. Assert the BeadDetailSheet appears (check for sheet/drawer container becoming visible). Assert bead name and status displayed in the sheet.

    3. `test('bead status colors reflect state')` — Seed beads with different statuses (pending=one, active=one, completed=one, failed=one). Navigate. Assert status badges/indicators have appropriate visual classes or aria-labels for each status.

    4. `test('SSE delivers bead status update to DAG (per D-15)')` — Seed project with one bead in 'pending' status. Navigate to execution page. Assert bead shows as pending. Then insert a `bead_dispatched` event directly into the `events` table via `createTestEvent(db, projectId, 'bead_dispatched', { beadId: beadId })`. Wait with `expect(locator).toBeVisible({ timeout: 8000 })` for the UI to reflect the status change (the 2s SSE polling interval + render time). This tests the full LISTEN/NOTIFY-less polling pipeline per D-15.

    5. `test('execution page has no accessibility violations')` — Seed data, navigate, `assertNoA11yViolations(page)`.

    **Teardown:** `truncateE2EDb(db)` in afterEach. Close client in afterAll.

    **Avoid `networkidle`** — the SSE connection keeps the network active.

    **Note on DAG rendering:** `@xyflow/react` renders to SVG/Canvas. Bead nodes may be custom React components rendered inside React Flow. Use data-testid attributes if available, or locate by text content. Read BeadNode.tsx to understand the rendered DOM structure for selectors.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && test -f packages/web/e2e/execution.spec.ts && grep -q 'assertNoA11yViolations' packages/web/e2e/execution.spec.ts && grep -q 'toHaveScreenshot' packages/web/e2e/execution.spec.ts && grep -q 'createTestEvent' packages/web/e2e/execution.spec.ts && grep -q 'timeout.*8000\|timeout.*5000\|timeout.*10000' packages/web/e2e/execution.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/e2e/execution.spec.ts` exists with >= 4 test blocks
    - File calls `assertNoA11yViolations` (D-03)
    - File calls `toHaveScreenshot` (D-02)
    - File calls `createTestEvent` with bead event type for SSE test (D-15)
    - File uses timeout >= 5000ms for SSE wait assertions (covers 2s poll interval)
    - File does NOT contain `waitForLoadState('networkidle')`
    - File calls `truncateE2EDb` in afterEach (D-06)
    - File does NOT send Inngest events or attempt live dispatch
  </acceptance_criteria>
  <done>Execution page E2E spec with DAG rendering, bead interaction, and SSE live update verification</done>
</task>

<task type="auto">
  <name>Task 2: E2E evolution and costs specs</name>
  <files>
    packages/web/e2e/evolution.spec.ts
    packages/web/e2e/costs.spec.ts
  </files>
  <read_first>
    packages/web/e2e/helpers/db.ts
    packages/web/e2e/helpers/accessibility.ts
    packages/web/e2e/helpers/routes.ts
    packages/web/src/app/projects/[id]/evolution/page.tsx
    packages/web/src/app/projects/[id]/costs/page.tsx
    packages/web/src/components/evolution/SeedLineageTree.tsx
    packages/web/src/components/evolution/ConvergencePanel.tsx
    packages/web/src/components/evolution/EvolutionTimeline.tsx
    packages/shared/src/db/schema/index.ts
  </read_first>
  <action>
    **Create `packages/web/e2e/evolution.spec.ts` (per D-01, D-04, D-13):**

    **Setup:** Create DB connection, run migrations. Seed project + seed (original) + evolved seed (with parentSeedId pointing to original) + events of type `evolution_started`, `evolution_converged`, `seed_crystallized`.

    **Tests:**
    1. `test('evolution page shows seed lineage tree')` — Seed project with 2 seeds (parent + child). Navigate to `ROUTES.evolution(projectId)`. Assert seed lineage tree visible with both seed versions. `assertNoA11yViolations(page)`. `expect(page).toHaveScreenshot('evolution-lineage.png', { threshold: 0.1, animations: 'disabled' })`.

    2. `test('convergence panel displays signal statuses')` — Seed project with convergence event in events table (type `evolution_converged` with payload containing signals array). Navigate. Assert convergence panel renders signal names.

    3. `test('evolution timeline shows cycle history')` — Seed project with multiple evolution events. Navigate. Assert timeline entries rendered with cycle numbers or timestamps.

    4. `test('evolution page has no accessibility violations')` — Seed data, navigate, `assertNoA11yViolations(page)`.

    **Create `packages/web/e2e/costs.spec.ts` (per D-01, D-04, D-13):**

    **Setup:** Seed project + llm_usage records (insert rows with model name, token counts, cost values). Read the `llm_usage` schema to understand required columns.

    **Tests:**
    1. `test('costs page displays token usage summary')` — Seed project + llm_usage records. Navigate to `ROUTES.costs(projectId)`. Assert total cost value displayed. Assert model breakdown visible. `assertNoA11yViolations(page)`. `expect(page).toHaveScreenshot('costs-page.png', { threshold: 0.1, animations: 'disabled' })`.

    2. `test('costs page shows per-model breakdown')` — Seed with usage for 2 different models. Navigate. Assert both model names appear in the breakdown.

    3. `test('costs page handles empty state')` — Seed project with no llm_usage records. Navigate. Assert empty state or zero-cost display.

    4. `test('costs page has no accessibility violations')` — Seed data, navigate, `assertNoA11yViolations(page)`.

    **Teardown:** `truncateE2EDb(db)` in afterEach for both specs.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && test -f packages/web/e2e/evolution.spec.ts && test -f packages/web/e2e/costs.spec.ts && grep -q 'assertNoA11yViolations' packages/web/e2e/evolution.spec.ts && grep -q 'assertNoA11yViolations' packages/web/e2e/costs.spec.ts && grep -q 'toHaveScreenshot' packages/web/e2e/evolution.spec.ts && grep -q 'toHaveScreenshot' packages/web/e2e/costs.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/e2e/evolution.spec.ts` exists with >= 3 test blocks
    - `packages/web/e2e/costs.spec.ts` exists with >= 3 test blocks
    - Both files call `assertNoA11yViolations` (D-03)
    - Both files call `toHaveScreenshot` (D-02)
    - Both files call `truncateE2EDb` in afterEach (D-06)
    - `evolution.spec.ts` seeds parent + child seeds for lineage test
    - `costs.spec.ts` seeds llm_usage records for cost display test
  </acceptance_criteria>
  <done>Evolution and costs E2E specs created with accessibility, snapshots, and proper DB seeding</done>
</task>

</tasks>

<verification>
- All 3 spec files exist in `packages/web/e2e/`
- Combined with Plan 03: all 6 E2E specs cover every dashboard surface (D-01 complete)
- Every spec has axe-core checks (D-03)
- Every spec has visual snapshots (D-02)
- Execution spec tests SSE live update (D-15)
</verification>

<success_criteria>
E2E tests for execution, evolution, and costs pages are created. All 6 dashboard surfaces now have E2E coverage. DAG rendering tested with pre-seeded data. SSE live update verified via DB insert.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-testing-e2e-testing-and-final-checks/17-04-SUMMARY.md`
</output>
