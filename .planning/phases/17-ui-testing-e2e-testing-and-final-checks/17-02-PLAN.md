---
phase: 17-ui-testing-e2e-testing-and-final-checks
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/web/src/__tests__/components/interview/ChatBubble.test.tsx
  - packages/web/src/__tests__/components/interview/MCChipGroup.test.tsx
  - packages/web/src/__tests__/components/interview/AmbiguityMeter.test.tsx
  - packages/web/src/__tests__/components/interview/SeedApprovalCard.test.tsx
  - packages/web/src/__tests__/components/interview/HoldoutCard.test.tsx
  - packages/web/src/__tests__/components/interview/ClarityBanner.test.tsx
  - packages/web/src/__tests__/components/dag/DAGCanvas.test.tsx
  - packages/web/src/__tests__/components/dag/BeadNode.test.tsx
  - packages/web/src/__tests__/components/dag/MoleculeGroup.test.tsx
  - packages/web/src/__tests__/components/evolution/SeedLineageTree.test.tsx
  - packages/web/src/__tests__/components/evolution/EvolutionTimeline.test.tsx
  - packages/web/src/__tests__/components/evolution/ConvergencePanel.test.tsx
  - packages/web/src/__tests__/components/bead/BeadDetailSheet.test.tsx
  - packages/web/src/__tests__/components/bead/DiffViewer.test.tsx
  - packages/web/src/__tests__/components/bead/TerminalPane.test.tsx
  - packages/web/src/__tests__/components/shell/NavSidebar.test.tsx
  - packages/web/src/__tests__/pages/interview-page.test.tsx
  - packages/web/src/__tests__/pages/execution-page.test.tsx
  - packages/web/src/__tests__/pages/evolution-page.test.tsx
  - packages/web/src/__tests__/pages/costs-page.test.tsx
  - packages/web/src/__tests__/pages/settings-page.test.tsx
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12
  - D-14

must_haves:
  truths:
    - "Every page and key component has a React component test"
    - "Component tests verify user interactions (click, type, submit), not just rendering"
    - "SSE streaming is tested via mocked EventSource"
    - "All component tests pass via pnpm -F @get-cauldron/web test"
  artifacts:
    - path: "packages/web/src/__tests__/components/interview/ChatBubble.test.tsx"
      provides: "ChatBubble render + interaction tests"
      min_lines: 20
    - path: "packages/web/src/__tests__/components/dag/DAGCanvas.test.tsx"
      provides: "DAGCanvas render test with SSE mock"
      min_lines: 20
    - path: "packages/web/src/__tests__/pages/interview-page.test.tsx"
      provides: "Interview page test with tRPC mock"
      min_lines: 30
  key_links:
    - from: "packages/web/src/__tests__/pages/interview-page.test.tsx"
      to: "packages/web/src/app/projects/[id]/interview/page.tsx"
      via: "vi.mock('@/trpc/client')"
      pattern: "vi.mock.*trpc"
    - from: "packages/web/src/__tests__/components/dag/DAGCanvas.test.tsx"
      to: "packages/web/src/__tests__/helpers/sse-mock.ts"
      via: "installEventSourceMock"
      pattern: "installEventSourceMock"
---

<objective>
Create comprehensive React component tests for all pages and key components in the web package, with full interaction testing and SSE mock coverage.

Purpose: Satisfies D-10 (all pages/components tested), D-11 (interaction testing), D-12 (SSE mock), D-14 (__tests__/ directory pattern). Provides the component-level safety net complementing E2E tests.
Output: 21 test files covering 16 components + 5 pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-testing-e2e-testing-and-final-checks/17-01-SUMMARY.md
@packages/web/vitest.config.ts
@packages/web/src/__tests__/setup.ts
@packages/web/src/__tests__/helpers/sse-mock.ts
@packages/web/src/__tests__/helpers/trpc-wrapper.tsx
@packages/web/src/trpc/client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component tests for interview and shell components (10 files)</name>
  <files>
    packages/web/src/__tests__/components/interview/ChatBubble.test.tsx
    packages/web/src/__tests__/components/interview/MCChipGroup.test.tsx
    packages/web/src/__tests__/components/interview/AmbiguityMeter.test.tsx
    packages/web/src/__tests__/components/interview/SeedApprovalCard.test.tsx
    packages/web/src/__tests__/components/interview/HoldoutCard.test.tsx
    packages/web/src/__tests__/components/interview/ClarityBanner.test.tsx
    packages/web/src/__tests__/components/bead/BeadDetailSheet.test.tsx
    packages/web/src/__tests__/components/bead/DiffViewer.test.tsx
    packages/web/src/__tests__/components/bead/TerminalPane.test.tsx
    packages/web/src/__tests__/components/shell/NavSidebar.test.tsx
  </files>
  <read_first>
    packages/web/src/components/interview/ChatBubble.tsx
    packages/web/src/components/interview/MCChipGroup.tsx
    packages/web/src/components/interview/AmbiguityMeter.tsx
    packages/web/src/components/interview/SeedApprovalCard.tsx
    packages/web/src/components/interview/HoldoutCard.tsx
    packages/web/src/components/interview/ClarityBanner.tsx
    packages/web/src/components/bead/BeadDetailSheet.tsx
    packages/web/src/components/bead/DiffViewer.tsx
    packages/web/src/components/bead/TerminalPane.tsx
    packages/web/src/components/shell/NavSidebar.tsx
    packages/web/src/__tests__/helpers/sse-mock.ts
    packages/web/src/__tests__/helpers/trpc-wrapper.tsx
  </read_first>
  <action>
    Create 10 component test files following these patterns:

    **For each component test file:**
    1. Read the component source to identify its props interface, event handlers, and rendered output
    2. Use `render()` from `@testing-library/react`, `screen`, `fireEvent` or `userEvent`
    3. Mock external dependencies (tRPC, SSE, @xyflow/react) via `vi.mock()`
    4. Test both rendering AND interaction (per D-11)
    5. Follow `__tests__/` directory convention (per D-14)

    **Interview components:**
    - `ChatBubble.test.tsx`: Render with role='user' and role='assistant' props. Assert text content appears. Assert role-based styling classes.
    - `MCChipGroup.test.tsx`: Render with options array. Assert all options rendered. fireEvent.click on an option, assert onSelect callback fires with correct value. Test freeform input submission.
    - `AmbiguityMeter.test.tsx`: Render with scores object (goalClarity, constraintClarity, successCriteriaClarity). Assert dimension labels rendered. Assert progress bar widths reflect scores.
    - `SeedApprovalCard.test.tsx`: Render with summary text. Assert summary displayed. fireEvent.click approve button, assert onApprove fires. fireEvent.click reject, assert onReject fires.
    - `HoldoutCard.test.tsx`: Render with holdout scenario data. Assert scenario text displayed. Test approve/reject buttons.
    - `ClarityBanner.test.tsx`: Render with clarity score. Assert score value displayed. Test threshold display (shows "ready" when <= 0.2).

    **Bead components:**
    - `BeadDetailSheet.test.tsx`: Mock sheet/dialog UI. Render with bead data. Assert bead name, status, logs displayed.
    - `DiffViewer.test.tsx`: Render with diff string. Assert diff lines rendered with +/- markers.
    - `TerminalPane.test.tsx`: Render with log lines array. Assert all lines appear. Test auto-scroll behavior.

    **Shell:**
    - `NavSidebar.test.tsx`: Render with project name. Assert nav links present (Interview, Execution, Evolution, Costs, Settings). Test link active state based on pathname mock.

    **Important patterns:**
    - Mock `next/navigation` for components using `useParams`/`usePathname`: `vi.mock('next/navigation', () => ({ useParams: () => ({ id: 'test-id' }), usePathname: () => '/projects/test-id/interview' }))`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm -F @get-cauldron/web test</automated>
  </verify>
  <acceptance_criteria>
    - 10 test files exist under `packages/web/src/__tests__/components/` for interview, bead, and shell
    - Each test file contains at least one `describe` block and at least 2 `test` or `it` blocks
    - `packages/web/src/__tests__/components/interview/MCChipGroup.test.tsx` contains `fireEvent.click` or `userEvent.click`
    - `packages/web/src/__tests__/components/shell/NavSidebar.test.tsx` contains assertions for link text (Interview, Execution, Evolution, Costs, Settings)
    - `pnpm -F @get-cauldron/web test` exits 0
  </acceptance_criteria>
  <done>10 component test files for interview, bead, and shell created with render + interaction assertions, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Component tests for DAG and evolution components (6 files)</name>
  <files>
    packages/web/src/__tests__/components/dag/DAGCanvas.test.tsx
    packages/web/src/__tests__/components/dag/BeadNode.test.tsx
    packages/web/src/__tests__/components/dag/MoleculeGroup.test.tsx
    packages/web/src/__tests__/components/evolution/SeedLineageTree.test.tsx
    packages/web/src/__tests__/components/evolution/EvolutionTimeline.test.tsx
    packages/web/src/__tests__/components/evolution/ConvergencePanel.test.tsx
  </files>
  <read_first>
    packages/web/src/components/dag/BeadNode.tsx
    packages/web/src/components/dag/DAGCanvas.tsx
    packages/web/src/components/dag/MoleculeGroup.tsx
    packages/web/src/components/evolution/SeedLineageTree.tsx
    packages/web/src/components/evolution/EvolutionTimeline.tsx
    packages/web/src/components/evolution/ConvergencePanel.tsx
    packages/web/src/__tests__/helpers/sse-mock.ts
    packages/web/src/__tests__/helpers/trpc-wrapper.tsx
  </read_first>
  <action>
    Create 6 component test files:

    **DAG components:**
    - `BeadNode.test.tsx`: Render with bead data (name, status). Assert name displayed. Assert status badge class matches status value (pending/active/completed/failed).
    - `MoleculeGroup.test.tsx`: Render with molecule name and child count. Assert molecule label rendered.
    - `DAGCanvas.test.tsx`: Mock `@xyflow/react` (`ReactFlow`, `useReactFlow`, `ReactFlowProvider`). Use `installEventSourceMock()` from helpers (per D-12). Render with bead array. Assert ReactFlow receives nodes. Test that bead click fires onNodeClick.

    **Evolution components:**
    - `SeedLineageTree.test.tsx`: Render with seed chain array. Assert each seed version displayed.
    - `EvolutionTimeline.test.tsx`: Render with evolution events. Assert timeline entries rendered with cycle numbers.
    - `ConvergencePanel.test.tsx`: Render with convergence signals. Assert signal names and statuses displayed.

    **Important patterns:**
    - Mock `@xyflow/react` in DAG tests: `vi.mock('@xyflow/react', () => ({ ReactFlow: (props) => <div data-testid="react-flow" />, ...}))`
    - Mock `next/navigation` for components using `useParams`/`usePathname`
    - For SSE-dependent components (DAGCanvas), use `installEventSourceMock()` from `src/__tests__/helpers/sse-mock.ts`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm -F @get-cauldron/web test</automated>
  </verify>
  <acceptance_criteria>
    - 6 test files exist under `packages/web/src/__tests__/components/` for dag and evolution
    - `packages/web/src/__tests__/components/dag/DAGCanvas.test.tsx` imports from `../helpers/sse-mock` (D-12)
    - Each test file contains at least one `describe` block and at least 2 `test` or `it` blocks
    - `pnpm -F @get-cauldron/web test` exits 0
  </acceptance_criteria>
  <done>6 component test files for DAG and evolution created with SSE mock and render + interaction assertions, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Page-level component tests with tRPC mocks</name>
  <files>
    packages/web/src/__tests__/pages/interview-page.test.tsx
    packages/web/src/__tests__/pages/execution-page.test.tsx
    packages/web/src/__tests__/pages/evolution-page.test.tsx
    packages/web/src/__tests__/pages/costs-page.test.tsx
    packages/web/src/__tests__/pages/settings-page.test.tsx
  </files>
  <read_first>
    packages/web/src/app/projects/[id]/interview/page.tsx
    packages/web/src/app/projects/[id]/execution/page.tsx
    packages/web/src/app/projects/[id]/evolution/page.tsx
    packages/web/src/app/projects/[id]/costs/page.tsx
    packages/web/src/app/projects/[id]/settings/page.tsx
    packages/web/src/trpc/client.tsx
    packages/web/src/__tests__/helpers/trpc-wrapper.tsx
    packages/web/src/__tests__/helpers/sse-mock.ts
  </read_first>
  <action>
    Create 5 page-level component tests. Each page test:
    1. Mocks `@/trpc/client` via `vi.mock('@/trpc/client', () => ({ useTRPC: () => mockTRPC }))` where mockTRPC matches the router shape the page uses
    2. Mocks `next/navigation` with `useParams: () => ({ id: 'test-project-id' })` and `usePathname`
    3. Wraps render in `QueryClientProvider` with `retry: false` (from `TestProviders` helper)
    4. Tests both initial render state and user interactions

    **interview-page.test.tsx:**
    - Mock `useTRPC().interview.getTranscript.queryOptions` to return `{ status: 'active', transcript: [{ role: 'assistant', content: 'What do you want to build?' }] }`
    - Mock `useTRPC().interview.sendAnswer.mutationOptions` to return a mutationFn
    - Assert: chat messages render, input field present, submit button present
    - Test interaction: type into textarea, click send, assert mutationFn called with answer text
    - Test auto-start: when status='not_started', assert startInterview mutation fires

    **execution-page.test.tsx:**
    - Mock `useTRPC().execution.getBeads.queryOptions` to return array of test beads with statuses
    - Install SSE mock from helpers
    - Assert: DAG canvas area rendered, bead count label matches
    - Test: click a bead node, assert detail sheet opens (mock the sheet component if needed)

    **evolution-page.test.tsx:**
    - Mock `useTRPC().evolution.getLineage.queryOptions` to return seed chain
    - Mock `useTRPC().evolution.getConvergence.queryOptions` to return convergence signals
    - Assert: seed lineage tree renders, convergence panel renders

    **costs-page.test.tsx:**
    - Mock `useTRPC().costs.getUsage.queryOptions` to return token usage data
    - Assert: cost breakdown table renders, total cost displayed
    - Test: filter by cycle if page supports it

    **settings-page.test.tsx:**
    - Mock `useTRPC().projects.getSettings.queryOptions` to return project settings
    - Mock `useTRPC().projects.archive.mutationOptions`
    - Assert: project name displayed, model config shown
    - Test: click archive button, assert confirmation dialog appears

    **Important mock patterns:**
    - Use `vi.mock('@/trpc/client')` with factory function
    - Pages may use `useQuery` or `useSuspenseQuery` -- read the page source to determine which
    - Mock `@tanstack/react-query` `useQuery`/`useSuspenseQuery` if the page uses them directly
    - For pages using `'use client'` directive, render directly (no server component issues in jsdom)
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm -F @get-cauldron/web test</automated>
  </verify>
  <acceptance_criteria>
    - 5 page test files exist under `packages/web/src/__tests__/pages/`
    - `interview-page.test.tsx` contains `vi.mock('@/trpc/client'` and tests for sendAnswer interaction
    - `execution-page.test.tsx` imports SSE mock helper
    - `settings-page.test.tsx` tests archive button click
    - All tests in `pnpm -F @get-cauldron/web test` pass (exit 0)
  </acceptance_criteria>
  <done>5 page tests created with tRPC mocks and interaction assertions, all passing</done>
</task>

</tasks>

<verification>
- `pnpm -F @get-cauldron/web test` exits 0
- `find packages/web/src/__tests__ -name "*.test.tsx" | wc -l` returns >= 22 (21 new + 1 existing smoke)
</verification>

<success_criteria>
All pages and key components have React component tests with full interaction coverage. Tests follow __tests__/ directory pattern. SSE streaming tested via mock. All pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-testing-e2e-testing-and-final-checks/17-02-SUMMARY.md`
</output>
