---
phase: quick
plan: 260327-rk9
type: execute
wave: 1
depends_on: []
files_modified: [README.md, CONTRIBUTING.md]
autonomous: true
requirements: []
---

<objective>
Write a compelling README.md and minimal CONTRIBUTING.md for the Cauldron repository.

Purpose: Cauldron's v1 milestone is complete (17 phases, 65 plans). The repo needs public-facing documentation that communicates what Cauldron is, how it works, and how to get started.
Output: README.md and CONTRIBUTING.md at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Create README.md at repo root with these sections in order:

1. **Header** -- Project name "Cauldron", one-line tagline: AI-powered software development platform that turns a description into tested, validated code. Include badges placeholder comment (CI, license, Node version).

2. **What is Cauldron?** -- 2-3 paragraph overview. Core value proposition: user describes what they want, Cauldron autonomously designs, decomposes, implements, tests, evaluates, and evolves until goal is met. Emphasize: Socratic requirements gathering, immutable specs, cross-model holdout testing, parallel DAG execution, evolutionary refinement. Humans steer at decision points, not babysit.

3. **Architecture** -- ASCII diagram showing the pipeline flow:
```
User (CLI / Web UI)
    |
    v
+------------------+     +------------------+     +------------------+
|    Interview     | --> |   Crystallize    | --> |      Seal        |
| Socratic Q&A     |     | Immutable Seed   |     | Encrypted Tests  |
+------------------+     +------------------+     +------------------+
                                                         |
    +----------------------------------------------------+
    |
    v
+------------------+     +------------------+     +------------------+
|   Decompose      | --> |    Execute       | --> |    Evaluate      |
| Parallel DAG     |     | Durable Agents   |     | Holdout Tests    |
+------------------+     +------------------+     +------------------+
                                                         |
                                                         v
                                                  +------------------+
                                                  |     Evolve       |
                                                  | Iterative Refine |
                                                  +------------------+
```
Adapt this layout for visual clarity. Below the diagram, add a brief (1-2 sentences per item) explanation of each stage.

4. **Key Features** -- Bulleted list:
   - Socratic Interview: Multi-perspective AI questioning (researcher, simplifier, architect, breadth-keeper, seed-closer) with ambiguity scoring
   - Immutable Seeds: Crystallized specs that cannot be modified after approval
   - Cross-Model Holdout Testing: Encrypted test scenarios (AES-256-GCM) invisible to implementation agents, enforcing model diversity
   - Parallel DAG Execution: 2-pass task decomposition into dependency-aware bead graph, dispatched via Inngest durable steps
   - Evolutionary Loop: Tiered mutation strategy with lateral thinking personas and meta-judge convergence
   - LLM Gateway: Multi-provider routing (OpenAI, Anthropic, Google) with failover, circuit breaking, and budget enforcement
   - Web Dashboard: Real-time DAG visualization, SSE streaming, interview UI
   - CLI: Full pipeline control from terminal

5. **Monorepo Structure** -- Show the 4-package layout from CLAUDE.md with brief descriptions and the dependency graph line.

6. **Tech Stack** -- Concise table (same as CLAUDE.md tech stack table).

7. **Quickstart** -- Prerequisites section (Node.js 22+, pnpm 10+, Docker). Then numbered steps:
   1. Clone the repo
   2. `pnpm install`
   3. Copy `.env.example` to `.env` and fill in API keys (note: OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY for multi-provider support)
   4. `docker compose up -d` (Postgres, Redis, Inngest)
   5. `pnpm db:migrate`
   6. `pnpm dev` for web dashboard at localhost:3000
   7. Or use CLI: `pnpm -F @get-cauldron/cli cauldron interview start`

8. **CLI Commands** -- Reference table of key commands:
   - `cauldron interview start` -- Begin Socratic requirements session
   - `cauldron interview resume` -- Continue an existing interview
   - `cauldron crystallize` -- Lock interview into immutable seed
   - `cauldron run` -- Execute full pipeline (seal, decompose, execute, evaluate)
   - `cauldron status` -- Show project/bead status
   - `cauldron logs` -- Stream execution logs (SSE)
   - `cauldron evolve` -- Trigger evolution cycle
   Note: all CLI commands run via `pnpm -F @get-cauldron/cli cauldron <command>`

9. **Development** -- Brief section:
   - `pnpm build` / `pnpm typecheck` / `pnpm lint` / `pnpm test`
   - Integration tests need Docker Postgres on :5433
   - E2E tests via Playwright
   - Link to CONTRIBUTING.md

10. **License** -- Placeholder: "License TBD" (user hasn't specified yet)

Style: Professional but energetic. Use clear headers. No excessive marketing language. Let the architecture speak for itself. Keep total length under ~300 lines.
  </action>
  <verify>
    <automated>test -f /Users/zakkeown/Code/cauldron/README.md && wc -l /Users/zakkeown/Code/cauldron/README.md | awk '{if ($1 > 50 && $1 < 400) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <done>README.md exists at repo root with all 10 sections, ASCII architecture diagram, quickstart guide, and CLI reference. Between 100-350 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Write CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create a minimal CONTRIBUTING.md at repo root covering:

1. **Header** -- "Contributing to Cauldron"

2. **Development Setup** -- Reference the Quickstart in README, then add:
   - Run `docker compose up -d` for infrastructure
   - `pnpm db:migrate` for database
   - `pnpm dev` starts all dev servers

3. **Project Structure** -- Brief pointer to the monorepo layout in README and CLAUDE.md for detailed architecture.

4. **Code Standards**:
   - TypeScript end-to-end (no JavaScript files)
   - Vitest for unit tests, Playwright for E2E
   - Integration tests use real Postgres (Docker :5433), do not mock the database
   - Run `pnpm typecheck && pnpm lint && pnpm test && pnpm build` before submitting

5. **Branching and PRs**:
   - Fork the repo, create a feature branch
   - Keep PRs focused (one feature/fix per PR)
   - Include tests for new functionality
   - Ensure CI passes

6. **Key Conventions**:
   - Use `postgres` driver (not `pg`)
   - Use `@xyflow/react` (not `react-flow-renderer`)
   - Use `@dagrejs/dagre` (not `dagre` 0.8.x)
   - Hono for API workers, tRPC for internal dashboard only
   - See "Do Not Use" section in CLAUDE.md for banned dependencies

Keep it concise -- under 100 lines. This is a minimal starting point.
  </action>
  <verify>
    <automated>test -f /Users/zakkeown/Code/cauldron/CONTRIBUTING.md && wc -l /Users/zakkeown/Code/cauldron/CONTRIBUTING.md | awk '{if ($1 > 20 && $1 < 150) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <done>CONTRIBUTING.md exists at repo root with development setup, code standards, PR guidelines, and key conventions. Between 40-120 lines.</done>
</task>

</tasks>

<verification>
Both files exist at repo root and contain expected sections:
- README.md has architecture diagram, quickstart, CLI reference
- CONTRIBUTING.md has setup, standards, conventions
</verification>

<success_criteria>
- README.md is compelling and complete with all 10 sections
- ASCII architecture diagram accurately represents the 7-stage pipeline
- Quickstart guide is actionable (clone to running in numbered steps)
- CLI commands reference covers the key commands
- CONTRIBUTING.md covers setup, standards, and conventions
- Both files are well-structured markdown that renders correctly
</success_criteria>

<output>
No summary file needed for quick tasks.
</output>
