# Quick Task 260328-g8l: Hide archived projects by default with toggle

## Task 1: Add `includeArchived` parameter to projects.list router

**Files:** `packages/web/src/trpc/routers/projects.ts`

Add optional `includeArchived` boolean input (default false). When false, filter out projects whose name starts with `[archived]` in addition to the existing `deletedAt` null check.

## Task 2: Add toggle to ProjectListClient

**Files:** `packages/web/src/app/projects/ProjectListClient.tsx`

Add a `useState` toggle for showing archived projects. Pass `includeArchived` to the tRPC query. Render a small toggle button in the list header area.
