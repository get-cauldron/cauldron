---
phase: quick
plan: 260331-a60
subsystem: cli-health
tags: [cli, health, preflight, infrastructure]
dependency_graph:
  requires: [server-check, shared-db-client, engine-server, codebase-memory-mcp]
  provides: [real-health-command, expanded-pre-execution-checklist, health-test-coverage]
  affects: [cli-health-command, local-execution-readiness]
tech_stack:
  added: []
  patterns: [fail-fast-preflight, redis-ping-check, binary-availability-check, inngest-post-probe]
key_files:
  created: []
  modified:
    - packages/cli/src/health.ts
    - packages/cli/src/cli.ts
    - packages/cli/src/__tests__/health.test.ts
decisions:
  - "Kept AI provider keys and HOLDOUT_ENCRYPTION_KEY as warnings, not hard failures, so health stays useful as a local preflight without forcing provider network validation"
  - "Used the known-good Inngest dev probe: POST /v1/events with an empty array payload"
metrics:
  duration: 8min
  completed: "2026-03-31T13:27:20Z"
---

# Quick Task 260331-a60: CLI Health Preflight Summary

**One-liner:** `cauldron health` now runs the real local pre-execution checklist instead of only checking whether the web server responds.

## What Was Done

### Task 1: Reconnect the health command to the real preflight (0d97053)

Updated the CLI router so the `health` command calls `healthCheck()` with the configured `serverUrl`, and updated the help text to describe it as a local pre-execution prerequisite check.

### Task 2: Expand the preflight checks and tests (0d97053)

Expanded `packages/cli/src/health.ts` to validate:
- required env: `DATABASE_URL`, `REDIS_URL`, `INNGEST_DEV=1`
- PostgreSQL connectivity and schema readiness
- Redis reachability
- Inngest dev server reachability via `POST /v1/events` with `[]`
- `codebase-memory-mcp` binary availability
- engine server reachability at `http://localhost:3001/api/inngest`
- web server reachability via the existing tRPC health probe

Also added optional warnings for missing AI provider keys and missing `HOLDOUT_ENCRYPTION_KEY`, and expanded the unit test coverage to lock in the new behavior.

## Deviations from Plan

None.

## Verification

- `pnpm --filter @get-cauldron/cli test -- src/__tests__/health.test.ts`
- `pnpm --filter @get-cauldron/cli typecheck`

## Self-Check: PASSED
