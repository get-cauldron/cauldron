---
phase: quick
plan: 260331-a60
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/health.ts
  - packages/cli/src/cli.ts
  - packages/cli/src/__tests__/health.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`cauldron health` runs the real pre-execution preflight instead of only checking web server reachability"
    - "Health check validates local execution prerequisites: env, PostgreSQL/schema, Redis, Inngest, codebase-memory-mcp, engine server, and web server"
    - "Health check uses the correct Inngest dev probe (`POST /v1/events` with empty payload)"
    - "CLI health tests cover the expanded prerequisite checks"
  artifacts:
    - path: "packages/cli/src/health.ts"
      provides: "Expanded pre-execution checklist and prerequisite validation"
    - path: "packages/cli/src/cli.ts"
      provides: "Health command routed through healthCheck()"
    - path: "packages/cli/src/__tests__/health.test.ts"
      provides: "Unit coverage for the expanded health behavior"
  key_links:
    - from: "packages/cli/src/cli.ts"
      to: "packages/cli/src/health.ts"
      via: "health command branch"
      pattern: "await healthCheck\\("
    - from: "packages/cli/src/health.ts"
      to: "packages/cli/src/server-check.ts"
      via: "web server readiness probe"
      pattern: "isServerRunning"
---

<objective>
Improve the CLI health check so it validates pre-execution prerequisites, including local services and codebase-memory-mcp availability, instead of only checking web server connectivity.

Purpose: make `cauldron health` match the real local execution dependencies and stop reporting a false sense of readiness.
Output: updated CLI health behavior plus tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reconnect `cauldron health` to the real preflight path</name>
  <files>packages/cli/src/cli.ts, packages/cli/src/health.ts</files>
  <action>
Route the `health` command through `healthCheck()` instead of the inline `isServerRunning()` branch. Update the user-facing help text so the command is described as a local pre-execution prerequisite check.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @get-cauldron/cli typecheck</automated>
  </verify>
  <done>`cauldron health` no longer stops at web server connectivity; it calls the expanded preflight implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the health checklist and its tests</name>
  <files>packages/cli/src/health.ts, packages/cli/src/__tests__/health.test.ts</files>
  <action>
Extend `healthCheck()` to validate required execution prerequisites with clear failure messages:
- required env: DATABASE_URL, REDIS_URL, INNGEST_DEV=1
- PostgreSQL connectivity + schema readiness
- Redis reachability
- Inngest dev server reachability using `POST /v1/events` with `[]`
- codebase-memory-mcp binary availability via `--version`
- engine server reachability at `http://localhost:3001/api/inngest`
- web server reachability via existing tRPC health probe

Retain optional warnings for missing AI provider keys and missing HOLDOUT_ENCRYPTION_KEY so the command is useful as a preflight without forcing network validation against model providers.

Update the health tests to cover the new checks and the new success message.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/cauldron && pnpm --filter @get-cauldron/cli test -- src/__tests__/health.test.ts</automated>
  </verify>
  <done>Health check enforces the actual local prerequisites and the unit tests cover the expanded behavior.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @get-cauldron/cli test -- src/__tests__/health.test.ts`
2. `pnpm --filter @get-cauldron/cli typecheck`
</verification>

<success_criteria>
- `cauldron health` calls `healthCheck()` instead of doing a standalone server ping
- Health check validates Redis, codebase-memory-mcp, engine server, and web server in addition to DB/schema/Inngest
- Inngest probe uses `POST /v1/events` with an empty array payload
- Updated tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260331-a60-improve-the-cli-health-check-so-it-valid/260331-a60-SUMMARY.md`
</output>
